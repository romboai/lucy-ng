---
phase: 57-skill-ux
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/lucy-ng/predict.md
  - ~/.claude/commands/lucy-ng/dereplicate.md
  - ~/.claude/commands/lucy-ng/sanitise.md
autonomous: true
requirements: [SKUX-03, SKUX-04, SKUX-05]

must_haves:
  truths:
    - "When HOSE code lookup returns no predictions, predict skill tells user to try canonical SMILES, check for unusual functional groups, and notes database coverage limitations"
    - "When dereplication returns 0 matches, dereplicate skill tells user to check formula spelling, try related formulas, and notes compound may not be in the 928K database"
    - "sanitise performs a dry-run first that reports findings without modifying files, then asks for user confirmation before proceeding"
  artifacts:
    - path: "~/.claude/commands/lucy-ng/predict.md"
      provides: "HOSE miss error recovery guidance"
      contains: "no predictions"
    - path: "~/.claude/commands/lucy-ng/dereplicate.md"
      provides: "Zero-match error recovery guidance"
      contains: "0 matches"
    - path: "~/.claude/commands/lucy-ng/sanitise.md"
      provides: "Dry-run step before modifications"
      contains: "dry-run"
  key_links:
    - from: "~/.claude/commands/lucy-ng/predict.md"
      to: "lucy predict c13 CLI"
      via: "error handling in present_results step"
      pattern: "no predictions|HOSE"
    - from: "~/.claude/commands/lucy-ng/dereplicate.md"
      to: "lucy dereplicate c13 CLI"
      via: "error handling in present_results step"
      pattern: "0 matches|no.*found"
    - from: "~/.claude/commands/lucy-ng/sanitise.md"
      to: "scan_and_redact step"
      via: "dry-run gate before modifications"
      pattern: "dry.run|confirmation"
---

<objective>
Add error recovery guidance to predict and dereplicate skills, and add a dry-run confirmation step to sanitise.

Purpose: Users hitting edge cases (no HOSE predictions, no database matches, accidental file modification) get actionable next steps instead of dead ends.
Output: Updated predict.md, dereplicate.md, and sanitise.md with error recovery and dry-run patterns.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v6.0-ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add HOSE miss guidance to predict.md and 0-match guidance to dereplicate.md</name>
  <files>~/.claude/commands/lucy-ng/predict.md, ~/.claude/commands/lucy-ng/dereplicate.md</files>
  <action>
**predict.md** — In the `present_results` step, expand the error handling section. After the existing "On error:" block, add a new section for partial/no predictions:

```
**If some atoms have no predictions (HOSE code miss):**
- Report which atom indices have no predictions
- Explain: "The HOSE-code database has 7.9M entries but may not cover unusual functional groups or rare substitution patterns."
- Suggest: "Try these alternatives:"
  1. "Use canonical SMILES (RDKit: Chem.MolToSmiles(mol)) — non-canonical SMILES may produce different atom numbering"
  2. "Simplify the structure — remove stereochemistry (/ and \ characters) and try again"
  3. "Check for unusual functional groups — the database is built from COCONUT and NMRShiftDB natural products, which may lack synthetic scaffolds"
- Note: "Atoms with 'low' confidence have few database examples. Consider the prediction range rather than the point estimate."
```

**dereplicate.md** — In the `present_results` step, after the existing error handling, add a new section for zero matches:

```
**If 0 matches returned (no compounds with this formula in database):**
- Report: "No compounds with formula [formula] found in the database (928K compounds from COCONUT + NMRShiftDB)."
- Suggest troubleshooting steps:
  1. "Check formula spelling — common mistakes: lowercase letters (use C14H16 not c14h16), missing element counts (C14H16O vs C14H16O1)"
  2. "Try related formulas — if unsure about heteroatom count, try ±1 oxygen or nitrogen (e.g., C14H16O, C14H18O2)"
  3. "The compound may not be in the database — COCONUT covers ~400K natural products and NMRShiftDB ~30K experimental entries. Synthetic compounds and rare natural products may be absent."
  4. "Consider running full CASE instead: /lucy-ng:case — this determines structure from NMR data without requiring database lookup"
```
  </action>
  <verify>
    <automated>grep -c "HOSE" ~/.claude/commands/lucy-ng/predict.md && grep -c "928K\|0 matches\|no compounds" ~/.claude/commands/lucy-ng/dereplicate.md</automated>
  </verify>
  <done>predict.md has a dedicated section for HOSE code misses with 3 actionable suggestions. dereplicate.md has a dedicated section for 0-match results with 4 troubleshooting steps including formula checking and CASE referral.</done>
</task>

<task type="auto">
  <name>Task 2: Add dry-run step to sanitise.md</name>
  <files>~/.claude/commands/lucy-ng/sanitise.md</files>
  <action>
Restructure the sanitise.md process to add a dry-run phase between scanning and modifying files. The current flow is: validate_input → discover_text_files → delete_structure_and_audit_files → scan_and_redact → present_report → verify_completeness → handoff_instructions.

**New flow:** validate_input → discover_text_files → dry_run_scan → present_dry_run_report → (user confirms) → delete_structure_and_audit_files → scan_and_redact → present_report → verify_completeness → handoff_instructions.

**Changes:**

1. **Rename `scan_and_redact` step** — keep it as-is but it now only runs AFTER user confirms the dry-run report.

2. **Add new step `dry_run_scan` after `discover_text_files`:**

```xml
<step name="dry_run_scan">
**Scan all text files and report findings WITHOUT modifying anything.**

For each file in files_to_read list:

1. **Read file content** using Read tool
2. **Apply semantic reasoning to identify compound identifiers** (same categories as scan_and_redact)
3. **Record each finding** — do NOT write any files

Build a findings manifest:
- Files that would be deleted (structure files, audit logs) — list with reasons
- Identifiers that would be redacted — list with file path, identifier text, and type
- Total counts: files to delete, identifiers to redact, files to modify

**CRITICAL:** This step is READ-ONLY. Do NOT use Write tool or rm commands. Do NOT modify any files.
</step>
```

3. **Add new step `present_dry_run_report` after `dry_run_scan`:**

```xml
<step name="present_dry_run_report">
**Present findings and require explicit user confirmation before proceeding.**

Present the dry-run report:

## Sanitisation Dry Run — [dataset_path]

### Files to Delete
[List structure files and audit logs that will be removed]

### Identifiers to Redact
| File | Identifier | Type | Action |
|------|-----------|------|--------|
| ... | ... | ... | Replace with [REDACTED] |

### Summary
- Files to delete: N
- Identifiers to redact: N across M files
- Files unchanged: N

**No changes have been made yet.**

Type "proceed" to apply these changes, or "cancel" to abort.

**STOP and wait for user response.** Do NOT continue to delete/redact steps until user explicitly confirms.
</step>
```

4. **Update `delete_structure_and_audit_files` step** — Add a note at the top: "This step runs only after user confirmation of the dry-run report."

5. **Update `scan_and_redact` step** — Add a note at the top: "This step runs only after user confirmation. The dry_run_scan step has already identified all targets — apply the recorded changes."
  </action>
  <verify>
    <automated>grep -c "dry.run\|dry_run\|proceed.*cancel\|No changes have been made" ~/.claude/commands/lucy-ng/sanitise.md</automated>
  </verify>
  <done>sanitise.md has a dry-run scan step that reads all files and reports findings without modification, followed by an explicit user confirmation gate ("proceed" or "cancel") before any files are deleted or redacted.</done>
</task>

</tasks>

<verification>
- predict.md present_results step has HOSE miss guidance with 3 suggestions
- dereplicate.md present_results step has 0-match guidance with 4 steps
- sanitise.md has dry_run_scan and present_dry_run_report steps before any file modifications
- sanitise.md requires explicit user confirmation ("proceed") before modifying files
</verification>

<success_criteria>
- predict.md guides users through HOSE code misses (alternative SMILES, database coverage note)
- dereplicate.md guides users through 0-match results (formula check, related formulas, CASE referral)
- sanitise.md performs a dry-run that reports findings without modifying files and requires "proceed" confirmation
</success_criteria>

<output>
After completion, create `.planning/phases/57-skill-ux/57-02-SUMMARY.md`
</output>
