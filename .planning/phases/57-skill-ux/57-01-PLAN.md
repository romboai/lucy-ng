---
phase: 57-skill-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/lucy-ng/lucy-ng.md
  - ~/.claude/commands/lucy-ng/status.md
  - ~/.claude/commands/lucy-ng/dereplicate.md
  - ~/.claude/commands/lucy-ng/predict.md
  - ~/.claude/commands/lucy-ng/sanitise.md
  - ~/.claude/commands/lucy-ng/case.md
autonomous: true
requirements: [SKUX-01, SKUX-02]

must_haves:
  truths:
    - "A user asking 'I have an unknown compound' gets routed to /lucy-ng:case"
    - "A user asking 'predict chemical shifts' gets routed to /lucy-ng:predict"
    - "A user asking 'is this compound in the database' gets routed to /lucy-ng:dereplicate"
    - "A user asking 'prepare for blind testing' gets routed to /lucy-ng:sanitise"
    - "A user asking 'is my setup ready' gets routed to /lucy-ng:status"
    - "Each skill frontmatter description contains natural-language trigger phrases"
    - "The routing page has at least 4 decision branches"
  artifacts:
    - path: "~/.claude/commands/lucy-ng/lucy-ng.md"
      provides: "Decision tree routing page"
      contains: "decision tree"
    - path: "~/.claude/commands/lucy-ng/case.md"
      provides: "Updated description with trigger phrases"
    - path: "~/.claude/commands/lucy-ng/predict.md"
      provides: "Updated description with trigger phrases"
    - path: "~/.claude/commands/lucy-ng/dereplicate.md"
      provides: "Updated description with trigger phrases"
    - path: "~/.claude/commands/lucy-ng/sanitise.md"
      provides: "Updated description with trigger phrases"
    - path: "~/.claude/commands/lucy-ng/status.md"
      provides: "Updated description with trigger phrases"
  key_links:
    - from: "~/.claude/commands/lucy-ng/lucy-ng.md"
      to: "all 5 sub-commands"
      via: "decision tree branches"
      pattern: "lucy-ng:(case|predict|dereplicate|sanitise|status)"
---

<objective>
Optimize skill discoverability by adding natural-language trigger phrases to all 5 skill descriptions and building a decision tree routing page.

Purpose: Users reaching lucy-ng through natural language ("I have an unknown compound", "predict shifts for this molecule") get routed to the correct sub-command without needing to know the command names.
Output: Updated lucy-ng.md routing page with decision tree, updated frontmatter descriptions for all 5 skills.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v6.0-ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add natural-language trigger phrases to all 5 skill descriptions</name>
  <files>~/.claude/commands/lucy-ng/case.md, ~/.claude/commands/lucy-ng/predict.md, ~/.claude/commands/lucy-ng/dereplicate.md, ~/.claude/commands/lucy-ng/sanitise.md, ~/.claude/commands/lucy-ng/status.md</files>
  <action>
Update the `description` field in the YAML frontmatter of each skill file to include natural-language trigger phrases that match how users actually ask. The description is what Claude uses to match user intent to the correct sub-command.

**case.md** — Change description to:
"Full CASE workflow — autonomous NMR structure elucidation. Use when: unknown compound, determine structure, identify molecule, what is this compound, structure determination from NMR"

**predict.md** — Change description to:
"Predict 13C NMR chemical shifts for a SMILES structure. Use when: predict shifts, expected chemical shifts, what shifts would this have, HOSE code prediction, verify a structure"

**dereplicate.md** — Change description to:
"Match 13C NMR spectrum against compound database. Use when: is this compound in the database, identify known compound, dereplication, match spectrum, compare shifts to database"

**sanitise.md** — Change description to:
"Remove compound identifiers from NMR datasets for blind CASE evaluation. Use when: prepare blind test, remove names, anonymise dataset, sanitise for CASE, blind testing"

**status.md** — Change description to:
"Check lucy-ng environment readiness. Use when: is lucy-ng installed, check setup, verify environment, is LSD available, database status"

Only modify the `description` field in frontmatter. Do NOT change any other frontmatter fields or any content below the frontmatter.
  </action>
  <verify>
    <automated>for f in case predict dereplicate sanitise status; do echo "=== $f ===" && head -5 ~/.claude/commands/lucy-ng/$f.md; done</automated>
  </verify>
  <done>All 5 skill files have updated description fields containing natural-language trigger phrases. Each description includes "Use when:" followed by at least 4 trigger phrases.</done>
</task>

<task type="auto">
  <name>Task 2: Build decision tree routing page in lucy-ng.md</name>
  <files>~/.claude/commands/lucy-ng/lucy-ng.md</files>
  <action>
Rewrite lucy-ng.md to include a decision tree that guides users to the correct sub-command. Keep the existing command table but add a decision tree section BEFORE the Quick Start section.

The decision tree must have at least 4 branches. Structure it as:

```markdown
## What do you want to do?

**I have an unknown compound and want to determine its structure from NMR data**
→ `/lucy-ng:case <compound_path> <formula>` — Full autonomous CASE workflow with 4-agent team

**I have a known structure (SMILES) and want to predict its 13C shifts**
→ `/lucy-ng:predict "<smiles>"` — HOSE-code based 13C shift prediction

**I have a 13C spectrum and want to check if the compound is in the database**
→ `/lucy-ng:dereplicate <bruker_path> <formula>` — Match spectrum against 928K compounds

**I want to prepare an NMR dataset for blind CASE evaluation**
→ `/lucy-ng:sanitise <dataset_path>` — Remove compound names, CAS numbers, structure files

**I want to check if my environment is ready**
→ `/lucy-ng:status` — Verify lucy-ng CLI, LSD solver, and database are installed
```

Keep the existing command table after the decision tree. Update the Quick Start section to reference the decision tree ("Not sure which command? See the decision tree above.").
  </action>
  <verify>
    <automated>grep -c "→" ~/.claude/commands/lucy-ng/lucy-ng.md</automated>
  </verify>
  <done>lucy-ng.md contains a decision tree with 5 branches (at least 4 required), each pointing to the correct sub-command with a brief description. The routing page helps users who describe their goal in natural language find the right command.</done>
</task>

</tasks>

<verification>
- All 5 skill descriptions contain "Use when:" trigger phrases
- lucy-ng.md decision tree has at least 4 branches
- No skill file content (below frontmatter) was inadvertently modified in Task 1
</verification>

<success_criteria>
- Each of the 5 skill files has a description with natural-language trigger phrases covering: NMR, structure determination, unknown compound, blind testing, chemical shifts (distributed across appropriate skills)
- The routing page decision tree has 5 branches guiding users to case, predict, dereplicate, sanitise, and status
</success_criteria>

<output>
After completion, create `.planning/phases/57-skill-ux/57-01-SUMMARY.md`
</output>
