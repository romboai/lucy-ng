---
phase: 53-agent-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/agents/lucy-lsd-engineer.md
  - ~/.claude/agents/lucy-devils-advocate.md
  - ~/.claude/commands/lucy-ng/case.md
autonomous: true
requirements: [AGNT-01, AGNT-02, AGNT-03, AGNT-04]

must_haves:
  truths:
    - "lsd-engineer workflow includes a fragment search step that runs lucy fragment search before each iteration and injects DEFF/FEXP before MULT in the LSD file"
    - "lsd-engineer has zero-solution recovery protocol that discards fragment and logs conflict when fragment causes 0 solutions"
    - "Constraint inventory JSON schema includes a deff_fexp section with status, fragment_smiles, fragment_filename, and conflict_reason fields"
    - "Devils-advocate checks fragment .lsd file existence on disk when DEFF F command is present in LSD file"
    - "Devils-advocate validates deff_fexp inventory field matches actual DEFF F/FEXP commands in LSD file"
    - "CASE-PROGRESS.md format includes Fragment search and Fragment file fields in LSD-Engineer iteration sections"
  artifacts:
    - path: "~/.claude/agents/lucy-lsd-engineer.md"
      provides: "Fragment search workflow step, DEFF/FEXP domain knowledge, inventory schema extension, updated message template"
      contains: "lucy fragment search"
    - path: "~/.claude/agents/lucy-devils-advocate.md"
      provides: "Fragment file existence check, fragment ordering check, inventory deff_fexp validation"
      contains: "Fragment File Existence"
    - path: "~/.claude/commands/lucy-ng/case.md"
      provides: "Fragment search and fragment file fields in CASE-PROGRESS.md write_progress format"
      contains: "Fragment search"
  key_links:
    - from: "~/.claude/agents/lucy-lsd-engineer.md"
      to: "lucy fragment search CLI"
      via: "workflow step 4a fragment search"
      pattern: "lucy fragment search --shifts"
    - from: "~/.claude/agents/lucy-lsd-engineer.md"
      to: "lucy fragment to-lsd CLI"
      via: "workflow step 4a fragment file generation"
      pattern: "lucy fragment to-lsd"
    - from: "~/.claude/agents/lucy-lsd-engineer.md"
      to: "~/.claude/agents/lucy-devils-advocate.md"
      via: "deff_fexp inventory field shared between writer and validator"
      pattern: "deff_fexp"
    - from: "~/.claude/agents/lucy-devils-advocate.md"
      to: "~/.claude/commands/lucy-ng/case.md"
      via: "DEFF FEXP field in validation message written to CASE-PROGRESS.md"
      pattern: "DEFF FEXP"
---

<objective>
Integrate the fragment search and DEFF/FEXP injection pipeline into the CASE agent team by modifying three markdown agent instruction files: lsd-engineer (workflow + inventory), devils-advocate (validation), and case.md orchestrator (progress logging).

Purpose: Enable lsd-engineer to automatically search for spectral fragments and inject them as LSD goodlist constraints, with full inventory tracking and pre-run validation by devils-advocate. This is the final integration step before multi-compound UAT.

Output: Three updated markdown agent definition files with fragment search workflow, validation protocol, and progress logging format.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v5.0-ROADMAP.md
@.planning/STATE.md
@.planning/phases/53-agent-integration/53-RESEARCH.md
@~/.claude/agents/lucy-lsd-engineer.md
@~/.claude/agents/lucy-devils-advocate.md
@~/.claude/commands/lucy-ng/case.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fragment search workflow and inventory schema to lsd-engineer and devils-advocate</name>
  <files>~/.claude/agents/lucy-lsd-engineer.md, ~/.claude/agents/lucy-devils-advocate.md</files>
  <action>
**File 1: `~/.claude/agents/lucy-lsd-engineer.md`**

Make these ADDITIVE changes (do not restructure existing content):

1. **Section 1 (Domain Knowledge) -- add new subsection after "### Badlist Filters (DEFF NOT)" (after the "Exception: Remove epoxide..." line and the CRITICAL persistence note, before "### Manual Checklist"):**

   Add a new subsection `### Fragment Goodlist (DEFF/FEXP)` containing:
   - CLI commands: `lucy fragment search --shifts "<shifts>" --format json --top 1` and `lucy fragment to-lsd "<smiles>" --output-dir analysis/iteration_NN/ --format json`
   - Key JSON output fields: `result_count`, `fragments[0].smiles`, `fragments[0].atom_count`, `fragments[0].avg_deviation` from search; `filename`, `deff_command`, `fexp_command` from to-lsd
   - LSD file ordering rule: `DEFF F1 "filename.lsd"` and `FEXP "F1"` go AFTER the inventory comment block but BEFORE the first MULT command. This is different from `DEFF NOT` which goes after correlations.
   - Fragment persistence rule: carry DEFF F1/FEXP forward across iterations, same as DEFF NOT. Read from previous LSD file, never reconstruct.
   - Zero-solution fallback protocol: If LSD returns 0 solutions AND a fragment was injected: (1) remove DEFF F1 and FEXP lines, (2) re-run LSD without fragment, (3) if solutions return, fragment conflicts -- discard it, (4) update inventory deff_fexp.status to "discarded" with conflict_reason, (5) log in [ITERATION-COMPLETE] message
   - Anti-pattern: NEVER inject multiple fragments (`--top 1` only, FRAG-05 deferred)
   - Anti-pattern: Fragment .lsd file MUST be in the same `iteration_NN/` directory as `compound.lsd` (pass `--output-dir` explicitly)

2. **Section 1 "### Manual Checklist" -- add item 10:**

   After item 9 ("Constraint Inventory block present..."), add:
   ```
   10. If fragment applied: DEFF F1/FEXP present after inventory block but before first MULT, fragment .lsd file exists in iteration dir
   ```

3. **Section 5A (JSON Schema Reference table) -- add row:**

   Add a new row to the schema table:
   | `deff_fexp` | object or null | Fragment goodlist tracking: status, smiles, filename, commands, conflict_reason |

4. **Section 5B (LSD File Format example) -- update inventory JSON example:**

   Add `deff_fexp` field to the existing JSON example between `deff_not_patterns` and `detection_results`:
   ```
   ;   "deff_fexp": {
   ;     "status": "applied",
   ;     "fragment_smiles": "Cc1ccccc1",
   ;     "fragment_atoms": 7,
   ;     "fragment_avgdev": 0.45,
   ;     "fragment_filename": "fragment_abc123def456.lsd",
   ;     "deff_command": "DEFF F1 \"fragment_abc123def456.lsd\"",
   ;     "fexp_command": "FEXP \"F1\"",
   ;     "search_result_count": 3,
   ;     "conflict_reason": null
   ;   },
   ```

   Also add DEFF F1/FEXP lines after the `; === END CONSTRAINT INVENTORY ===` line and before the first MULT line in the example:
   ```
   DEFF F1 "fragment_abc123def456.lsd"
   FEXP "F1"
   ```

5. **Section 5C (Initialization Procedure) -- add step between existing steps 5 and 6 (or after detection results, before writing):**

   Add a fragment search sub-step:
   ```
   5a. Run fragment search: `lucy fragment search --shifts "<13c_shifts>" --format json --top 1`
       - If result_count > 0: run `lucy fragment to-lsd "<smiles>" --output-dir analysis/iteration_01/ --format json`, populate deff_fexp with status "applied" and all fields
       - If result_count == 0: set deff_fexp to {"status": "none", ...null fields...}
       - DEFF F1/FEXP lines go after inventory block, before first MULT
   ```

6. **Section 5D (Update Procedure) -- add to the update-only-these-fields list:**

   Add a bullet:
   ```
   - Copy deff_fexp from previous inventory. If previous status was "applied", carry forward DEFF F1/FEXP lines. Re-run fragment search for logging. If previous was "discarded", keep "discarded" status.
   ```

7. **Workflow section -- insert new step between step 4 and step 5:**

   Renumber step 5 ("Build/update LSD file") to step 6, and all subsequent steps +1. Insert new step 5:
   ```
   5. Fragment search (run at start of EVERY iteration):
      a. Get 13C shift list from nmr-chemist's peak assignments
      b. Run: lucy fragment search --shifts "<comma_separated_shifts>" --format json --top 1
      c. Parse JSON: check result_count
      d. If result_count == 0: set deff_fexp = {"status": "none"}, skip to next step
      e. If result_count > 0:
         - Extract best fragment: fragments[0].smiles, atom_count, avg_deviation
         - Run: lucy fragment to-lsd "<smiles>" --output-dir analysis/iteration_NN/ --format json
         - Parse JSON: extract filename, deff_command, fexp_command
         - Set deff_fexp = {"status": "applied", ...populate all fields...}
         - DEFF/FEXP lines go in LSD file AFTER inventory block, BEFORE first MULT
   ```

8. **[ITERATION-COMPLETE] Message Template -- add 3 new fields:**

   After `Solution count: <N>` line and before `Constraints added:` line, add:
   ```
   Fragment search: <result_count> matches; applied rank #1: <SMILES> (<atoms> atoms, AVGDEV <dev> ppm)
     OR: No matching fragments found
     OR: Applied rank #1: <SMILES> -- DISCARDED (zero solutions with fragment)
   Fragment file: <filename> OR N/A
   ```

   In the `Constraint inventory delta:` line, append `, DEFF_FEXP=applied/none/discarded` after `BOND=N`.

**File 2: `~/.claude/agents/lucy-devils-advocate.md`**

Make these ADDITIVE changes:

1. **Section 3 (Structural Integrity Checks) -- add 2 new subsections after "### ELIM Usage" and before "### Aromatic Ring Expectation":**

   Add `### Fragment File Existence`:
   ```
   If the LSD file contains a `DEFF F` command (goodlist fragment reference, NOT `DEFF NOT`):
   1. Extract filename from between double quotes in the DEFF F command
   2. Check that the file exists in the same directory as compound.lsd
   3. If file missing: flag CRITICAL -- "DEFF references fragment file '<filename>' but file does not exist in <iteration_dir>/"
   4. If file exists: verify it contains SSTR commands (basic sanity check)

   If no DEFF F command found, check inventory deff_fexp.status:
   - If "applied": flag CRITICAL -- "Inventory claims fragment applied but no DEFF F command found in LSD file"
   - If "discarded" or "none" or null: OK (no fragment expected)
   ```

   Add `### Fragment Ordering`:
   ```
   If DEFF F and FEXP commands are present:
   1. Get line number of DEFF F command
   2. Get line number of first MULT command
   3. If DEFF F line > first MULT line: flag CRITICAL -- "DEFF F1 must appear BEFORE MULT definitions (goodlist ordering rule)"
   4. Get line number of FEXP command
   5. If FEXP line > first MULT line: flag CRITICAL -- "FEXP must appear BEFORE MULT definitions"
   ```

2. **Section 5B (Three-Check Reconciliation Protocol) -- extend Check 1 and Check 2:**

   In the Check 1 table, add a new row:
   | `deff_fexp.status == "applied"` | `grep -c "^DEFF F" compound.lsd` == 1 AND `grep -c "^FEXP" compound.lsd` == 1 | Both present if applied |

   Add note after Check 1 table: "If deff_fexp.status is 'none' or 'discarded', verify `grep -c "^DEFF F" compound.lsd` == 0 (no stale fragment references)."

   In the Check 2 section, add:
   ```
   | `deff_fexp` | Track status changes | Previous "applied" -> current missing/null = CRITICAL. Previous "applied" -> current "discarded" = INFO (normal zero-solution fallback, verify conflict_reason populated). Previous "none" -> current "applied" = normal (fragment found). |
   ```

3. **[VALIDATION-PASSED] template -- add field after "Grouped notation:" and before "Correlation order:":**

   ```
   DEFF FEXP: <filename> exists in iteration_NN/ (verified)
     OR: No fragment applied (deff_fexp.status = "none")
     OR: Fragment discarded (deff_fexp.status = "discarded", conflict logged)
   ```

   After the existing "Correlation order:" line, add:
   ```
   Fragment ordering: DEFF F1/FEXP before MULT (correct) OR N/A (no fragment)
   ```

4. **[VALIDATION-BLOCKED] template -- add field in CRITICAL issues area:**

   Add example critical issue:
   ```
   - Fragment file '<filename>' referenced by DEFF but not found in iteration dir
   ```
  </action>
  <verify>
Verify the modifications are present with grep checks:

For lsd-engineer.md:
- `grep -c "lucy fragment search" ~/.claude/agents/lucy-lsd-engineer.md` returns >= 2 (domain knowledge + workflow step)
- `grep -c "lucy fragment to-lsd" ~/.claude/agents/lucy-lsd-engineer.md` returns >= 2 (domain knowledge + workflow step)
- `grep -c "deff_fexp" ~/.claude/agents/lucy-lsd-engineer.md` returns >= 3 (schema, initialization, update, example)
- `grep -c "Fragment Goodlist" ~/.claude/agents/lucy-lsd-engineer.md` returns 1
- `grep -c "DEFF F1" ~/.claude/agents/lucy-lsd-engineer.md` returns >= 2 (example + checklist)
- `grep -c "Fragment search:" ~/.claude/agents/lucy-lsd-engineer.md` returns >= 1 (message template)
- `grep "Fragment file:" ~/.claude/agents/lucy-lsd-engineer.md` shows the field in the message template
- `grep "DEFF_FEXP=" ~/.claude/agents/lucy-lsd-engineer.md` shows it in the inventory delta line

For devils-advocate.md:
- `grep -c "Fragment File Existence" ~/.claude/agents/lucy-devils-advocate.md` returns 1
- `grep -c "Fragment Ordering" ~/.claude/agents/lucy-devils-advocate.md` returns 1
- `grep -c "deff_fexp" ~/.claude/agents/lucy-devils-advocate.md` returns >= 2 (check 1 + check 2 extensions)
- `grep -c "DEFF FEXP" ~/.claude/agents/lucy-devils-advocate.md` returns >= 1 (validation template)
- `grep "Fragment ordering:" ~/.claude/agents/lucy-devils-advocate.md` shows the field in the validation template
- `grep -c "DEFF F" ~/.claude/agents/lucy-devils-advocate.md` returns >= 3 (file existence check, ordering check, inventory check)
  </verify>
  <done>
lsd-engineer.md contains: Fragment Goodlist subsection with CLI commands and ordering rule, deff_fexp in inventory schema, fragment search workflow step, zero-solution recovery protocol, updated message template with Fragment search/Fragment file/DEFF_FEXP fields, manual checklist item 10. devils-advocate.md contains: Fragment File Existence check, Fragment Ordering check, deff_fexp validation in Check 1 and Check 2, DEFF FEXP and Fragment ordering fields in validation templates.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add fragment logging to case.md orchestrator and verify all three files</name>
  <files>~/.claude/commands/lucy-ng/case.md</files>
  <action>
**File: `~/.claude/commands/lucy-ng/case.md`**

Make these ADDITIVE changes to the `write_progress` step:

1. **Section 4 (LSD-Engineer section format) -- add 2 new fields:**

   After the existing `**Solution count:** <from message>` line and before `**Constraints added:**`, add:
   ```markdown
   **Fragment search:** <from message>
   **Fragment file:** <from message>
   ```

   These map directly to the new fields in lsd-engineer's [ITERATION-COMPLETE] message template.

2. **Section 5 (Devils-Advocate section format) -- add 2 new fields:**

   After the existing `**DEFF NOT:** <from message>` line and before `**SYME:**`, add:
   ```markdown
   **DEFF FEXP:** <from message>
   ```

   After `**Correlation order:** <from message>` and before `**Concerns:**` (or at end of DA fields if Concerns is last), add:
   ```markdown
   **Fragment ordering:** <from message>
   ```

3. **Backward-compatibility note (at end of write_progress step) -- extend the field list:**

   Add to the bullet list of parsed field names:
   ```
   - `Fragment search:` -- in ### LSD-Engineer
   - `Fragment file:` -- in ### LSD-Engineer
   ```

   These are new fields so there is no backward-compatibility concern, but document them for completeness.

**After all three files are modified, run final verification:**

Verify cross-file consistency:
- lsd-engineer's [ITERATION-COMPLETE] template has "Fragment search:" and "Fragment file:" fields
- case.md write_progress has matching `**Fragment search:**` and `**Fragment file:**` fields
- lsd-engineer's inventory has "deff_fexp" field
- devils-advocate validates "deff_fexp" field
- lsd-engineer places DEFF F1/FEXP "before MULT"
- devils-advocate checks DEFF F1/FEXP "before MULT"

Check no accidental deletions: each file should be LONGER than before (pure additions).
- `wc -l ~/.claude/agents/lucy-lsd-engineer.md` should be > 367 (was 367)
- `wc -l ~/.claude/agents/lucy-devils-advocate.md` should be > 349 (was 349)
- `wc -l ~/.claude/commands/lucy-ng/case.md` should be > 1088 (was ~1088)
  </action>
  <verify>
For case.md:
- `grep -c "Fragment search" ~/.claude/commands/lucy-ng/case.md` returns >= 2 (LSD-Engineer section + backward-compat note)
- `grep -c "Fragment file" ~/.claude/commands/lucy-ng/case.md` returns >= 2 (LSD-Engineer section + backward-compat note)
- `grep -c "DEFF FEXP" ~/.claude/commands/lucy-ng/case.md` returns >= 1 (DA section)
- `grep -c "Fragment ordering" ~/.claude/commands/lucy-ng/case.md` returns >= 1 (DA section)

Cross-file consistency:
- `grep "Fragment search:" ~/.claude/agents/lucy-lsd-engineer.md` matches format in case.md
- `grep "deff_fexp" ~/.claude/agents/lucy-lsd-engineer.md ~/.claude/agents/lucy-devils-advocate.md` shows presence in both files
- `wc -l ~/.claude/agents/lucy-lsd-engineer.md` > 367
- `wc -l ~/.claude/agents/lucy-devils-advocate.md` > 349
- `wc -l ~/.claude/commands/lucy-ng/case.md` > 1088
  </verify>
  <done>
case.md write_progress step includes Fragment search and Fragment file fields in LSD-Engineer section, DEFF FEXP and Fragment ordering fields in Devils-Advocate section, and backward-compatibility note updated. All three files are longer than originals (pure additions), and cross-file field names are consistent (deff_fexp inventory field, Fragment search/Fragment file message fields, DEFF F1/FEXP ordering rule).
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the 5 success criteria from the roadmap:

1. **AGNT-01 (fragment search + DEFF/FEXP injection):**
   - `grep "lucy fragment search" ~/.claude/agents/lucy-lsd-engineer.md` shows CLI command in workflow
   - `grep "BEFORE.*MULT\|before.*MULT\|before first MULT" ~/.claude/agents/lucy-lsd-engineer.md` shows ordering rule

2. **AGNT-02 (zero-solution discard):**
   - `grep -A 5 "zero.solution" ~/.claude/agents/lucy-lsd-engineer.md` shows discard protocol
   - `grep "discarded" ~/.claude/agents/lucy-lsd-engineer.md` shows status value

3. **AGNT-03 (constraint inventory deff_fexp):**
   - `grep "deff_fexp" ~/.claude/agents/lucy-lsd-engineer.md` shows schema extension
   - `grep "deff_fexp" ~/.claude/agents/lucy-devils-advocate.md` shows validation

4. **AGNT-04 (DA fragment file check):**
   - `grep "Fragment File Existence" ~/.claude/agents/lucy-devils-advocate.md` shows new check
   - `grep "fragment.*file.*not.*exist\|file.*missing" ~/.claude/agents/lucy-devils-advocate.md` shows CRITICAL flag

5. **CASE-PROGRESS.md fragment logging:**
   - `grep "Fragment search" ~/.claude/commands/lucy-ng/case.md` shows field in write_progress
   - `grep "Fragment file" ~/.claude/commands/lucy-ng/case.md` shows field in write_progress
</verification>

<success_criteria>
- lsd-engineer.md has "Fragment Goodlist (DEFF/FEXP)" subsection, fragment search workflow step, deff_fexp inventory schema, zero-solution fallback, updated message template
- devils-advocate.md has Fragment File Existence check, Fragment Ordering check, deff_fexp inventory validation, updated validation templates
- case.md has Fragment search and Fragment file fields in LSD-Engineer progress section, DEFF FEXP and Fragment ordering in DA progress section
- All three files are strictly longer than originals (additive changes only)
- Cross-file consistency: inventory field name "deff_fexp" appears in both lsd-engineer (writer) and devils-advocate (validator); message template field "Fragment search:" in lsd-engineer matches progress format in case.md
</success_criteria>

<output>
After completion, create `.planning/phases/53-agent-integration/53-01-SUMMARY.md`
</output>
