---
phase: 26-thin-tools
plan: 04
type: execute
wave: 2
depends_on: ["26-01", "26-02", "26-03"]
files_modified:
  - CLAUDE.md
  - skill/SKILL.md
  - skill/CASE/SKILL.md
  - skill/diagnostic/SKILL.md
  - .claude/agents/supervisor.md
  - .claude/agents/diagnostic-specialist.md
autonomous: true

must_haves:
  truths:
    - "CLAUDE.md has no MCP references (no Tool Output Reference for MCP tools, no MCP permissions, no lucy-mcp)"
    - "skill/SKILL.md frontmatter lists no MCP tools; body references CLI commands via Bash"
    - "skill/CASE/SKILL.md workflow uses CLI commands (lucy pick hsqc, lucy pick hmbc, etc.) not MCP tools"
    - "Agent definitions (.claude/agents/*.md) do not reference MCP tool names"
    - "Thin CLI signatures documented in skill (single path + threshold for pick hsqc/hmbc, no lsd generate)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Project instructions with CLI-only architecture"
    - path: "skill/SKILL.md"
      provides: "CASE domain knowledge referencing thin CLI commands"
    - path: "skill/CASE/SKILL.md"
      provides: "CASE workflow using thin CLI + AI intelligence"
  key_links:
    - from: "skill/CASE/SKILL.md"
      to: "skill/SKILL.md"
      via: "Cross-reference for domain knowledge"
      pattern: "see.*skill/SKILL.md"
    - from: "skill/SKILL.md"
      to: "CLI commands"
      via: "Bash tool usage patterns"
      pattern: "lucy pick|lucy analyze|lucy dereplicate|lucy lsd"
---

<objective>
Update all documentation, skill files, and agent definitions to reflect the CLI-only architecture. Remove all MCP tool references and update to reference thin CLI commands via Bash.

Purpose: After MCP removal and CLI thinning (Plans 01-03), the skill documents and agent definitions still reference MCP tools that no longer exist. This plan aligns all documentation with the new architecture.

Output: CLAUDE.md, skill files, and agent definitions all describe a CLI-only thin-tool architecture where the AI is the intelligence layer.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-thin-tools/26-CONTEXT.md
@.planning/phases/26-thin-tools/26-01-SUMMARY.md
@.planning/phases/26-thin-tools/26-02-SUMMARY.md

Key files:
@CLAUDE.md — Has MCP Tool Output Reference table, Peak Picking API with DEPTGuidedPicker examples, MCP permissions in settings.json
@skill/SKILL.md — Frontmatter lists 15 MCP tools, body references pick_hsqc_peaks/pick_hmbc_peaks/analyze_symmetry/generate_lsd_input
@skill/CASE/SKILL.md — Workflow may reference MCP tools for peak picking and LSD generation
@skill/diagnostic/SKILL.md — Line 1146-1147 references "MCP tools" and pick_hmbc_peaks
@.claude/agents/supervisor.md — Routing logic may reference MCP tools
@.claude/agents/diagnostic-specialist.md — May reference MCP tools
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update CLAUDE.md for CLI-only architecture</name>
  <files>CLAUDE.md</files>
  <action>
Read the full CLAUDE.md and make these specific changes:

**1. Remove the Tool Output Reference table** (the table listing MCP tool names like read_spectrum_1d, pick_peaks_1d, pick_hsqc_peaks, etc. with their key output fields). Replace with a CLI Output Reference table showing the thin CLI commands:

```markdown
## CLI Output Reference

| Command | Key Output Fields (JSON) |
|---------|--------------------------|
| `lucy read 1d` | nucleus, frequency, ppm_range, data_points |
| `lucy read 2d` | experiment_type, nuclei, frequency, shape |
| `lucy pick 1d` | count, peaks (ppm, intensity) |
| `lucy pick 2d` | experiment_type, count, peaks (f1, f2, intensity) |
| `lucy pick hsqc` | experiment_type, count, peaks (f1, f2, intensity) |
| `lucy pick hmbc` | experiment_type, count, peaks (f1, f2, intensity) |
| `lucy analyze symmetry` | formula, expected_carbons, observed_peaks, difference |
| `lucy dereplicate c13` | is_match, top_matches (name, smiles, score) |
| `lucy predict c13` | predictions (atom_index, shift, confidence), success |
| `lucy lsd rank` | ranked_solutions (smiles, mae, quality, deviations) |
```

**2. Remove MCP-related sections from End-User Setup:**
- Remove step 5 about creating `.claude/settings.json` with MCP permissions (`Bash(lucy:*)` etc.)
- The permissions file is for MCP tool calls; CLI via Bash doesn't need it

**3. Update Peak Picking API Reference section:**
- This section shows Python API examples using DEPTGuidedPicker and HMBCGuidedPicker
- These Python APIs still exist in the library, BUT they are no longer the primary interface
- Add a note at the top: "These Python APIs are available for library use. The AI agent uses thin CLI commands via Bash instead — see skill/SKILL.md for the recommended workflow."
- Keep the examples as library reference but make clear CLI is primary

**4. Remove or update any remaining MCP references:**
- Search for "MCP", "mcp", "lucy-mcp", "MCP server", "Model Context Protocol" in the file
- Remove or rewrite any references to MCP tools being the interface
- If the intro mentions MCP, update to describe CLI-only architecture

**5. Remove `lucy lsd generate` from CLI Syntax Reference:**
- The "LSD Integration" section shows `lucy lsd generate` usage — remove those examples
- Add a note: "The AI agent writes LSD files directly using skill knowledge (see skill/CASE/SKILL.md)"

Do NOT remove: setup instructions, database reference, developer reference, technology stack. These are project-level content that stays in CLAUDE.md.
  </action>
  <verify>
```bash
# No MCP tool names in CLAUDE.md
! grep -q "pick_hsqc_peaks\|pick_hmbc_peaks\|generate_lsd_input\|analyze_symmetry\b\|lucy-mcp" CLAUDE.md && echo "No MCP tool references"

# No MCP permission settings
! grep -q "Bash(lucy:" CLAUDE.md && echo "No MCP permissions"

# lucy lsd generate not documented
! grep -q "lsd generate" CLAUDE.md && echo "No lsd generate reference"
```
  </verify>
  <done>CLAUDE.md reflects CLI-only architecture with no MCP references.</done>
</task>

<task type="auto">
  <name>Task 2: Update skill files and agent definitions for thin CLI</name>
  <files>skill/SKILL.md, skill/CASE/SKILL.md, skill/diagnostic/SKILL.md, .claude/agents/supervisor.md, .claude/agents/diagnostic-specialist.md</files>
  <action>
**Update skill/SKILL.md:**

1. **Frontmatter tools list** (lines 4-19): Remove the entire `tools:` list that names 15 MCP tools. Replace with a description that the skill works with CLI commands via Bash:
```yaml
---
name: lucy-ng
description: [keep existing description, but remove "MCP" mentions]
interface: CLI via Bash tool
commands:
  - lucy read 1d/2d
  - lucy pick 1d/2d/hsqc/hmbc
  - lucy analyze symmetry
  - lucy dereplicate c13
  - lucy predict c13
  - lucy lsd check/run/rank/analyze
  - lucy visualize correlations
  - lucy database info/download
  - lucy fetch nmrxiv
---
```

2. **Section 3 (Peak Picking Strategy)** — Update tool references:
   - Line ~173: Change "Use `pick_hsqc_peaks` with DEPT-135 as ground truth. Algorithm iteratively lowers HSQC threshold..." to explain the thin CLI approach: "Use `lucy pick hsqc <path>` to get raw HSQC peaks above threshold. Then apply DEPT-guided filtering yourself:" followed by the reasoning steps (match HSQC carbons to DEPT peaks from `lucy pick 1d <dept135_path>`, extract multiplicity from DEPT sign, etc.)
   - Line ~186: Change "Use `pick_hmbc_peaks` to cross-validate against known 13C and HSQC positions..." to: "Use `lucy pick hmbc <path>` to get raw HMBC peaks. Then apply cross-validation yourself:" followed by the reasoning steps
   - Remove Python API code examples (`DEPTGuidedPicker.pick_hsqc_peaks(...)`, `HMBCGuidedPicker.pick_hmbc_peaks_from_spectra(...)`) — replace with CLI command examples and AI reasoning instructions
   - Keep the tolerances and domain knowledge (±1.5 ppm carbon, ±0.1 ppm proton) — this is the intelligence the AI needs

3. **Section 4 (Symmetry Detection)** — Update:
   - Line ~212: Change "Use `analyze_symmetry` to detect and quantify discrepancies" to "Use `lucy analyze symmetry <formula> <c13_path>` to get raw observed vs expected carbon counts. Then reason about symmetry yourself:"
   - The AI reasoning steps should include: check if observed < expected, look for intensity patterns suggesting equivalence

4. **Quick Reference section** (lines ~537+):
   - Update all MCP tool references to CLI commands
   - `pick_hsqc_peaks` → `lucy pick hsqc <path>`
   - `pick_hmbc_peaks` → `lucy pick hmbc <path>`
   - `analyze_symmetry` → `lucy analyze symmetry <formula> <c13_path>`
   - `generate_lsd_input` → (removed — AI writes LSD directly)
   - Add note about AI writing LSD files directly

**Update skill/CASE/SKILL.md:**

Read the full file first. The key workflow steps to update:
- Step 2 (Peak Picking): Replace any MCP tool calls with CLI commands. AI picks raw peaks via CLI, then applies DEPT/HMBC filtering using SKILL.md knowledge
- Step 4 (LSD Generation): Remove reference to `lucy lsd generate` or `generate_lsd_input`. Replace with: "Write the LSD file directly using the LSD reference in skill/SKILL.md Section 6 and the diagnostic specialist's LSD command reference in skill/diagnostic/SKILL.md Section 1"
- Any other step referencing MCP tools → replace with CLI equivalents

**Update skill/diagnostic/SKILL.md:**
- Lines 1146-1147: Replace "MCP tools" reference and `pick_hmbc_peaks` with CLI equivalent
- Search for any other MCP references in the file

**Update .claude/agents/supervisor.md:**
- Verify tools list in frontmatter — keep Task, Read, Write, Bash, Glob, Grep (no MCP)
- Workflow routing section (line ~55+): Ensure all CLI commands are correct thin versions
  - `lucy dereplicate c13 <path> <formula>` — unchanged
  - Any reference to MCP tools → CLI equivalent
- Check for references to `generate_lsd_input`, `pick_hsqc_peaks`, etc. and replace with CLI commands

**Update .claude/agents/diagnostic-specialist.md:**
- Verify no MCP tool references
- It already uses Bash tool for `lucy lsd` commands — confirm correct
- Check line references to MCP tools and replace with CLI
  </action>
  <verify>
```bash
# No MCP tool names in skill files
! grep -q "pick_hsqc_peaks\|pick_hmbc_peaks\|generate_lsd_input\|read_spectrum_1d" skill/SKILL.md skill/CASE/SKILL.md && echo "No MCP tool names in skills"

# No MCP references in agent definitions
! grep -q "pick_hsqc_peaks\|pick_hmbc_peaks\|generate_lsd_input\|MCP" .claude/agents/supervisor.md .claude/agents/diagnostic-specialist.md && echo "No MCP in agents"

# skill/SKILL.md frontmatter no longer has tools: list with MCP names
! grep -q "pick_hsqc_peaks" skill/SKILL.md && echo "SKILL.md clean"

# diagnostic skill updated
! grep -q "MCP tools" skill/diagnostic/SKILL.md && echo "Diagnostic skill clean"
```
  </verify>
  <done>All skill files and agent definitions reference thin CLI commands via Bash. No MCP tool names remain anywhere in the documentation.</done>
</task>

</tasks>

<verification>
1. `grep -rn "pick_hsqc_peaks\|pick_hmbc_peaks\|generate_lsd_input\|read_spectrum_1d\|analyze_symmetry\b" CLAUDE.md skill/ .claude/agents/` returns zero matches for MCP tool names
2. `grep -rn "MCP\|mcp\|lucy-mcp" CLAUDE.md skill/ .claude/agents/` returns zero matches (or only in non-tool contexts like "Model Context Protocol" in historical comments)
3. skill/SKILL.md Quick Reference section shows CLI commands
4. skill/CASE/SKILL.md workflow uses `lucy pick hsqc <path>` not `pick_hsqc_peaks`
</verification>

<success_criteria>
- Zero MCP tool references in CLAUDE.md, skill/, and .claude/agents/
- All documentation describes CLI-only architecture
- Thin CLI command signatures documented with correct arguments
- AI reasoning steps documented where intelligence was removed from CLI
</success_criteria>

<output>
After completion, create `.planning/phases/26-thin-tools/26-04-SUMMARY.md`
</output>
