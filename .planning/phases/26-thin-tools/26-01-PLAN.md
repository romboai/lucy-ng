---
phase: 26-thin-tools
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lucy_ng/mcp/server.py
  - src/lucy_ng/mcp/__init__.py
  - pyproject.toml
  - tests/test_mcp_server.py
autonomous: true

must_haves:
  truths:
    - "No MCP server code exists in the codebase"
    - "lucy-mcp entry point is removed from pyproject.toml"
    - "MCP optional dependency group is removed from pyproject.toml"
    - "pytest passes without MCP-related test failures"
  artifacts:
    - path: "pyproject.toml"
      provides: "Clean project config with no MCP references"
      contains: "no lucy-mcp entry, no mcp optional dependency"
  key_links:
    - from: "pyproject.toml"
      to: "src/lucy_ng/cli/__init__.py"
      via: "project.scripts only has lucy entry point"
      pattern: "lucy = \"lucy_ng.cli:cli\""
---

<objective>
Remove the MCP server entirely from lucy-ng. Delete all MCP infrastructure files and clean up project configuration.

Purpose: The AI agent now uses CLI commands via Bash tool instead of MCP tool calls. MCP is dead weight -- removing it simplifies the architecture to a single interface (CLI).

Output: No `src/lucy_ng/mcp/` directory, no `lucy-mcp` script, no MCP dependency in pyproject.toml, and all tests pass.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-thin-tools/26-CONTEXT.md

Key files:
@src/lucy_ng/mcp/server.py — 1,282-line MCP server to delete
@src/lucy_ng/mcp/__init__.py — MCP module init to delete
@pyproject.toml — Contains lucy-mcp entry point and mcp optional dependency
@tests/test_mcp_server.py — MCP server tests to delete
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete MCP server and tests</name>
  <files>src/lucy_ng/mcp/server.py, src/lucy_ng/mcp/__init__.py, tests/test_mcp_server.py</files>
  <action>
Delete the entire MCP module and its tests:

1. Delete `src/lucy_ng/mcp/server.py` (1,282 lines — the MCP server with 15 tool definitions)
2. Delete `src/lucy_ng/mcp/__init__.py` (5 lines — imports mcp and main)
3. Delete the `src/lucy_ng/mcp/` directory itself (rmdir)
4. Delete `tests/test_mcp_server.py` (MCP-specific tests that will fail without the MCP package)

Do NOT delete any shared core modules that MCP imported (readers, processing, analysis, etc.) — those are still used by CLI.

Note: The MCP server imports `_find_database_path` from `cli/dereplicate.py` — that cross-layer import becomes irrelevant once MCP is gone. No action needed on the CLI side for this plan.
  </action>
  <verify>
```bash
# MCP directory should not exist
test ! -d src/lucy_ng/mcp && echo "MCP directory removed"

# MCP test should not exist
test ! -f tests/test_mcp_server.py && echo "MCP test removed"
```
  </verify>
  <done>The src/lucy_ng/mcp/ directory and tests/test_mcp_server.py no longer exist.</done>
</task>

<task type="auto">
  <name>Task 2: Clean up pyproject.toml</name>
  <files>pyproject.toml</files>
  <action>
Edit pyproject.toml to remove all MCP references:

1. Remove the `lucy-mcp` entry from `[project.scripts]`:
   - BEFORE: Has both `lucy = "lucy_ng.cli:cli"` and `lucy-mcp = "lucy_ng.mcp:main"`
   - AFTER: Only `lucy = "lucy_ng.cli:cli"` remains

2. Remove the `[project.optional-dependencies].mcp` group:
   - BEFORE: `mcp = ["mcp[cli]>=1.2.0"]`
   - AFTER: The entire `mcp` key is removed from optional-dependencies

Keep everything else unchanged (dev dependencies, prediction notes, build config, tool config).

3. Run `pip install -e .` to update the installed package (removes lucy-mcp entry point).

4. Verify the test suite passes:
   ```bash
   pytest tests/ -x --ignore=tests/test_mcp_server.py
   ```
   (test_mcp_server.py was already deleted in Task 1, but use --ignore defensively)

If any test imports from `lucy_ng.mcp`, those tests need to be deleted or have the import removed. Based on codebase inspection, only `test_mcp_server.py` imports from the MCP module.
  </action>
  <verify>
```bash
# No lucy-mcp in pyproject.toml
! grep -q "lucy-mcp" pyproject.toml && echo "lucy-mcp entry removed"

# No mcp optional dependency
! grep -q '"mcp\[cli\]' pyproject.toml && echo "MCP dependency removed"

# Tests pass
pytest tests/ -x -q 2>&1 | tail -5
```
  </verify>
  <done>pyproject.toml has no MCP references, pip install succeeds, and all remaining tests pass.</done>
</task>

</tasks>

<verification>
1. `ls src/lucy_ng/mcp/` returns "No such file or directory"
2. `grep -r "lucy-mcp\|from lucy_ng.mcp\|import.*mcp" pyproject.toml src/ tests/` returns no matches
3. `pytest tests/ -x -q` passes with no failures
4. `lucy --version` still works (CLI entry point intact)
</verification>

<success_criteria>
- MCP server code is fully removed (0 lines of MCP code in codebase)
- pyproject.toml references only the `lucy` CLI entry point
- All remaining tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/26-thin-tools/26-01-SUMMARY.md`
</output>
