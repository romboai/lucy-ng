---
phase: 27-sub-command-skills-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/lucy-ng/status.md
  - ~/.claude/commands/lucy-ng/dereplicate.md
autonomous: true

must_haves:
  truths:
    - "/lucy-ng:status reports lucy-ng version, LSD availability, and database presence"
    - "/lucy-ng:dereplicate wraps lucy dereplicate c13 and returns top matches with scores"
  artifacts:
    - path: "~/.claude/commands/lucy-ng/status.md"
      provides: "Environment readiness check skill"
      contains: "lucy --version"
    - path: "~/.claude/commands/lucy-ng/dereplicate.md"
      provides: "Dereplication wrapper skill"
      contains: "lucy dereplicate c13"
  key_links:
    - from: "~/.claude/commands/lucy-ng/status.md"
      to: "lucy CLI"
      via: "Bash tool executing lucy --version, lucy lsd check, lucy database info"
      pattern: "lucy (--version|lsd check|database info)"
    - from: "~/.claude/commands/lucy-ng/dereplicate.md"
      to: "lucy CLI"
      via: "Bash tool executing lucy dereplicate c13"
      pattern: "lucy dereplicate c13"
---

<objective>
Create the ~/.claude/commands/lucy-ng/ directory and implement the status and dereplicate sub-command skills.

Purpose: Establish the sub-command namespace directory and deliver two of three simple wrapper skills that call lucy CLI via Bash.
Output: Two working sub-command skill files following the established GSD pattern (YAML frontmatter + XML-tagged markdown body).
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-sub-command-skills-foundation/27-RESEARCH.md

Reference CLAUDE.md for CLI syntax:
- `lucy --version` returns version string
- `lucy lsd check` reports LSD and outlsd availability
- `lucy database info <path>` reports database stats
- `lucy dereplicate c13 <bruker_path_or_shifts> <formula> [-n N] [--format json]` returns matches
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create directory and status.md skill</name>
  <files>~/.claude/commands/lucy-ng/status.md</files>
  <action>
1. Create the namespace directory:
```bash
mkdir -p ~/.claude/commands/lucy-ng
```

2. Create `~/.claude/commands/lucy-ng/status.md` following the GSD pattern. The skill must:

- YAML frontmatter with fields: name (lucy-ng:status), description (one-line), allowed-tools ([Bash])
- No arguments needed (status checks the environment, not a specific compound)
- Body uses XML tags: `<objective>`, `<process>` with named `<step>` elements

Process steps:
- Step 1 "check_lucy": Run `lucy --version`. If not found, report "lucy-ng not installed. Run: pip install lucy-ng"
- Step 2 "check_lsd": Run `lucy lsd check`. Report LSD and outlsd availability. If missing, point to http://eos.univ-reims.fr/LSD/ and explain PATH setup
- Step 3 "check_database": Run `lucy database info data/reference/lucy-ng-derep.db`. If missing/error, report "Database not found. Run: lucy database download"
- Step 4 "report": Present combined status summary to user in a clear table format showing component, status (OK/MISSING), and action needed

Keep the file concise (~40-60 lines). This is a thin wrapper -- the skill just calls CLI commands and formats output. No domain intelligence.
  </action>
  <verify>
```bash
# File exists and has valid frontmatter
test -f ~/.claude/commands/lucy-ng/status.md && head -5 ~/.claude/commands/lucy-ng/status.md | grep -q "name: lucy-ng:status"
```
  </verify>
  <done>~/.claude/commands/lucy-ng/ directory exists, status.md contains YAML frontmatter with name/description/allowed-tools and process steps for checking version, LSD, and database</done>
</task>

<task type="auto">
  <name>Task 2: Create dereplicate.md skill</name>
  <files>~/.claude/commands/lucy-ng/dereplicate.md</files>
  <action>
Create `~/.claude/commands/lucy-ng/dereplicate.md` following the GSD pattern. The skill must:

- YAML frontmatter: name (lucy-ng:dereplicate), description (one-line about matching 13C spectrum against database), argument-hint ("<bruker_path_or_shifts> <formula> [-n N]"), allowed-tools ([Bash])
- Body uses XML tags: `<objective>`, `<process>` with named `<step>` elements

Process steps:
- Step 1 "parse_arguments": Parse user arguments. Two input modes:
  - Bruker path + formula: `lucy dereplicate c13 <bruker_path> <formula> [-n N]`
  - Shift list + formula: `lucy dereplicate c13 --shifts "<comma-separated>" <formula> [-n N]`
  - Default -n to 10 if not provided
  - If no arguments: show usage with both modes
- Step 2 "run_dereplication": Execute the CLI command with --format json flag for structured output:
  ```bash
  lucy dereplicate c13 [args] --format json
  ```
- Step 3 "present_results": Parse JSON output and present to user:
  - If is_match: report "MATCH FOUND" with compound name and SMILES
  - If not: show top N candidates in a table (rank, name, SMILES, score)
  - If error (e.g., no database): report error and suggest `lucy database download`

Keep the file concise (~50-70 lines). Thin wrapper -- no domain intelligence about interpreting results.
  </action>
  <verify>
```bash
# File exists with correct frontmatter
test -f ~/.claude/commands/lucy-ng/dereplicate.md && head -5 ~/.claude/commands/lucy-ng/dereplicate.md | grep -q "name: lucy-ng:dereplicate"
```
  </verify>
  <done>dereplicate.md contains YAML frontmatter with name/description/argument-hint/allowed-tools and process steps for both Bruker and shift-list input modes with JSON output parsing</done>
</task>

</tasks>

<verification>
Both files exist and follow established GSD pattern:
```bash
ls -la ~/.claude/commands/lucy-ng/
head -10 ~/.claude/commands/lucy-ng/status.md
head -10 ~/.claude/commands/lucy-ng/dereplicate.md
```

Each file should have:
- Valid YAML frontmatter (--- delimited) with name, description, allowed-tools
- XML-tagged body with <objective> and <process> sections
- Process steps that call lucy CLI via Bash
</verification>

<success_criteria>
1. Directory ~/.claude/commands/lucy-ng/ exists
2. status.md calls lucy --version, lucy lsd check, lucy database info and presents combined status
3. dereplicate.md accepts Bruker path or shift list with formula, calls lucy dereplicate c13 --format json, presents results
4. Both files follow GSD pattern (YAML frontmatter + XML body)
</success_criteria>

<output>
After completion, create `.planning/phases/27-sub-command-skills-foundation/27-01-SUMMARY.md`
</output>
