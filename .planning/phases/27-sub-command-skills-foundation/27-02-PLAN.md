---
phase: 27-sub-command-skills-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/lucy-ng/predict.md
  - ~/.claude/commands/lucy-ng/lucy-ng.md
autonomous: true

must_haves:
  truths:
    - "/lucy-ng:predict wraps lucy predict c13 and returns shift predictions with confidence"
    - "Invoking /lucy-ng shows a listing of all available sub-commands"
  artifacts:
    - path: "~/.claude/commands/lucy-ng/predict.md"
      provides: "13C shift prediction wrapper skill"
      contains: "lucy predict c13"
    - path: "~/.claude/commands/lucy-ng/lucy-ng.md"
      provides: "Routing page listing all sub-commands"
      contains: "/lucy-ng:status"
  key_links:
    - from: "~/.claude/commands/lucy-ng/predict.md"
      to: "lucy CLI"
      via: "Bash tool executing lucy predict c13"
      pattern: "lucy predict c13"
    - from: "~/.claude/commands/lucy-ng/lucy-ng.md"
      to: "sub-command skills"
      via: "listing of /lucy-ng:* commands"
      pattern: "/lucy-ng:(status|dereplicate|predict)"
---

<objective>
Create the predict sub-command skill and the routing page that lists all lucy-ng sub-commands.

Purpose: Complete the three simple wrapper skills and establish the routing page that replaces the old monolithic skill pattern.
Output: predict.md wrapper skill and lucy-ng.md routing page, both following GSD pattern.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/27-sub-command-skills-foundation/27-RESEARCH.md

Reference CLAUDE.md for CLI syntax:
- `lucy predict c13 "<smiles>" [--format json]` returns shift predictions with confidence
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create predict.md skill</name>
  <files>~/.claude/commands/lucy-ng/predict.md</files>
  <action>
Ensure the directory exists first:
```bash
mkdir -p ~/.claude/commands/lucy-ng
```

Create `~/.claude/commands/lucy-ng/predict.md` following the GSD pattern. The skill must:

- YAML frontmatter: name (lucy-ng:predict), description (one-line about predicting 13C shifts from SMILES), argument-hint ("<smiles>"), allowed-tools ([Bash])
- Body uses XML tags: `<objective>`, `<process>` with named `<step>` elements

Process steps:
- Step 1 "parse_arguments": Parse user arguments.
  - Required: SMILES string (quoted)
  - If no arguments: show usage example `lucy predict c13 "CCO"`
  - If SMILES provided: validate it's a non-empty string
- Step 2 "run_prediction": Execute CLI command with --format json:
  ```bash
  lucy predict c13 "<smiles>" --format json
  ```
- Step 3 "present_results": Parse JSON output and present to user:
  - Show predictions table: atom index, predicted shift (ppm), confidence level
  - Report overall prediction quality (how many atoms had predictions, average confidence)
  - If error (e.g., no database): report error and suggest `lucy database download`
  - If SMILES invalid: report RDKit parse error from CLI output

Keep the file concise (~40-60 lines). Thin wrapper -- no domain intelligence about interpreting predictions.
  </action>
  <verify>
```bash
# File exists with correct frontmatter
test -f ~/.claude/commands/lucy-ng/predict.md && head -5 ~/.claude/commands/lucy-ng/predict.md | grep -q "name: lucy-ng:predict"
```
  </verify>
  <done>predict.md contains YAML frontmatter with name/description/argument-hint/allowed-tools and process steps for SMILES input, JSON prediction output, and result presentation</done>
</task>

<task type="auto">
  <name>Task 2: Create routing page (lucy-ng.md)</name>
  <files>~/.claude/commands/lucy-ng/lucy-ng.md</files>
  <action>
Create `~/.claude/commands/lucy-ng/lucy-ng.md` as the namespace routing page. This replaces the old monolithic /lucy-ng skill with a directory that points to sub-commands.

- YAML frontmatter: name (lucy-ng), description ("Lucy-ng NMR structure elucidation - command listing")
- NO allowed-tools field needed (this is a routing page, not an executor)
- Body is plain markdown listing all sub-commands

Content structure:
```markdown
# Lucy-ng: AI-Powered NMR Structure Elucidation

Available sub-commands:

| Command | Description |
|---------|-------------|
| `/lucy-ng:status` | Check environment readiness (lucy-ng, LSD, database) |
| `/lucy-ng:dereplicate` | Match 13C spectrum against compound database |
| `/lucy-ng:predict` | Predict 13C chemical shifts for a SMILES structure |

## Quick Start

1. Check your environment: `/lucy-ng:status`
2. Try dereplication: `/lucy-ng:dereplicate data/compound/2 C14H16`
3. Try prediction: `/lucy-ng:predict "CCO"`

## Coming Soon

- `/lucy-ng:case` - Full CASE orchestration (Phase 29)
- `/lucy-ng:sanitise` - Remove compound identifiers for blind evaluation (Phase 31)
```

Keep this file short (~30-40 lines). It's a signpost, not documentation.

Note: The file is named `lucy-ng.md` (matching the namespace) so that `/lucy-ng` invocation shows this routing page. The research found that GSD uses /gsd:help for this purpose, but naming the file after the namespace is the most natural convention.
  </action>
  <verify>
```bash
# File exists with correct frontmatter
test -f ~/.claude/commands/lucy-ng/lucy-ng.md && head -5 ~/.claude/commands/lucy-ng/lucy-ng.md | grep -q "name: lucy-ng"
```
  </verify>
  <done>lucy-ng.md exists as routing page with name "lucy-ng" in frontmatter, lists all three sub-commands (status, dereplicate, predict) with descriptions and quick-start examples</done>
</task>

</tasks>

<verification>
All four files exist and follow established GSD pattern:
```bash
ls -la ~/.claude/commands/lucy-ng/
for f in ~/.claude/commands/lucy-ng/*.md; do
  echo "=== $(basename $f) ==="
  head -5 "$f"
  echo
done
```

Each skill file should have:
- Valid YAML frontmatter (--- delimited) with at minimum name and description
- Body content with process steps (wrapper skills) or command listing (routing page)
</verification>

<success_criteria>
1. predict.md calls lucy predict c13 --format json with SMILES argument and presents shift predictions
2. lucy-ng.md lists all three sub-commands (status, dereplicate, predict) with descriptions
3. Both files follow GSD pattern (YAML frontmatter)
4. All 4 files in ~/.claude/commands/lucy-ng/ form a complete sub-command namespace
</success_criteria>

<output>
After completion, create `.planning/phases/27-sub-command-skills-foundation/27-02-SUMMARY.md`
</output>
