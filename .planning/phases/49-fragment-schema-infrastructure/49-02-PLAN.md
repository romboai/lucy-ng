---
phase: 49-fragment-schema-infrastructure
plan: 02
type: execute
wave: 2
depends_on: [49-01]
files_modified:
  - src/lucy_ng/cli/fragment.py
  - src/lucy_ng/cli/main.py
autonomous: true
requirements: [FRAG-01]

must_haves:
  truths:
    - "lucy fragment info reports schema version, SSC count (0), bin size, and file path for an empty database"
    - "lucy fragment info errors with helpful message when database file does not exist"
    - "lucy fragment --help lists available subcommands"
    - "Existing lucy commands (dereplicate, predict, database) still work after adding fragment command group"
  artifacts:
    - path: "src/lucy_ng/cli/fragment.py"
      provides: "lucy fragment CLI command group with info subcommand"
      contains: "def fragment"
    - path: "src/lucy_ng/cli/main.py"
      provides: "Registration of fragment command group"
      contains: "cli.add_command(fragment)"
  key_links:
    - from: "src/lucy_ng/cli/fragment.py"
      to: "src/lucy_ng/fragments/db.py"
      via: "imports FragmentDatabaseManager for database access"
      pattern: "from lucy_ng\\.fragments"
    - from: "src/lucy_ng/cli/main.py"
      to: "src/lucy_ng/cli/fragment.py"
      via: "registers fragment command group"
      pattern: "from lucy_ng\\.cli\\.fragment import fragment"
---

<objective>
Create the `lucy fragment info` CLI command and register the `fragment` command group in the main CLI. This is the user-facing entry point for fragment database operations.

Purpose: Confirms the database infrastructure from Plan 01 is accessible via the CLI. Provides the `lucy fragment info` command that verifies database creation and reports statistics. Future phases (50-54) will add `build`, `search`, and `to-lsd` subcommands to this command group.

Output: `lucy fragment info` command working end-to-end, fragment group registered in main CLI.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/49-fragment-schema-infrastructure/49-RESEARCH.md
@.planning/phases/49-fragment-schema-infrastructure/49-01-SUMMARY.md
@src/lucy_ng/cli/main.py
@src/lucy_ng/cli/database.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create fragment CLI command group with info subcommand</name>
  <files>
    src/lucy_ng/cli/fragment.py
    src/lucy_ng/cli/main.py
  </files>
  <action>
**Create `src/lucy_ng/cli/fragment.py`** following the pattern of `cli/database.py`:

```python
"""CLI commands for fragment library management."""

from __future__ import annotations

from pathlib import Path

import click
```

Define `DEFAULT_FRAGMENTS_DB = Path("data/reference/lucy-ng-fragments.db")`

1. **Command group:**
```python
@click.group()
def fragment() -> None:
    """Fragment library management and search."""
```

2. **`info` subcommand:**
   - Accept `db_path` as a Click argument with `type=click.Path(path_type=Path)` and default value `DEFAULT_FRAGMENTS_DB`
   - CRITICAL: Before opening the database, check `if not db_path.exists()`. If the file does not exist, print an error message to stderr: `"Error: Fragment database not found: {db_path}\nRun 'lucy fragment build' to create it, or specify a path with 'lucy fragment info <path>'"` and call `raise click.Abort()`. Do NOT let SQLite silently create an empty file.
   - Open `FragmentDatabaseManager(db_path)` as context manager
   - Call `db.get_schema_version()` — if version is not 7, emit a warning: `"Warning: Expected schema version 7, found {version}. This may not be a fragment database."`
   - Display: database path, schema version, SSC count (formatted with commas), bin size (ppm), file size (MB with 1 decimal)
   - Output format:
     ```
     Fragment database: data/reference/lucy-ng-fragments.db
       Schema version: 7
       SSC count: 0
       Bin size: 2.0 ppm
       File size: 0.0 MB
     ```

**Modify `src/lucy_ng/cli/main.py`:**
   - Add import: `from lucy_ng.cli.fragment import fragment`
   - Add registration: `cli.add_command(fragment)`
   - Update the help text docstring to include `fragment` in the commands list:
     ```
     fragment    Fragment library (build, search, info)
     ```

Follow the existing pattern exactly: thin CLI functions, no business logic in CLI layer. The `import` for `FragmentDatabaseManager` should be inside the `info()` function body (lazy import) to avoid slowing down other CLI commands. Follow the pattern used in `cli/database.py` where imports like `DatabaseImporter` are at the top level — actually in this project top-level imports are the convention, so use top-level import: `from lucy_ng.fragments import FragmentDatabaseManager`.
  </action>
  <verify>
Run these commands in sequence:

1. `lucy fragment --help` — should show "Fragment library management and search." with `info` subcommand listed
2. `lucy fragment info --help` — should show help for info command with DB_PATH argument
3. `lucy fragment info /nonexistent/path.db` — should print error "Fragment database not found" and exit with error
4. Create a test database and verify info works:
```bash
python -c "
from lucy_ng.fragments import FragmentDatabaseManager
import tempfile, os
f = tempfile.NamedTemporaryFile(suffix='.db', delete=False)
f.close()
with FragmentDatabaseManager(f.name) as db:
    db.create_tables()
print(f.name)
" > /tmp/test_frag_db_path.txt
lucy fragment info $(cat /tmp/test_frag_db_path.txt)
rm $(cat /tmp/test_frag_db_path.txt)
```
Should output schema version 7, SSC count 0, bin size 2.0 ppm.

5. `lucy --help` — should list `fragment` alongside other commands
6. `lucy database info data/reference/lucy-ng-derep.db` — existing command still works (if database exists)
  </verify>
  <done>
`lucy fragment info` command reports schema version 7, SSC count 0, bin size 2.0 ppm for a newly created fragment database. Command errors helpfully when database file does not exist. Fragment command group is registered in main CLI alongside all existing commands. Existing CLI commands are unaffected.
  </done>
</task>

</tasks>

<verification>
1. `lucy fragment --help` — shows fragment command group with info subcommand
2. `lucy fragment info /nonexistent.db` — errors with helpful message, does NOT create the file
3. `lucy --help` — lists fragment alongside read, pick, analyze, dereplicate, predict, detect, lsd, visualize, fetch, database
4. `ruff check src/lucy_ng/cli/fragment.py` — no lint errors
5. `mypy src/lucy_ng/cli/fragment.py` — type checking passes
</verification>

<success_criteria>
- `lucy fragment info` reports schema version, SSC count (0), bin size, and file path for an initialized fragment database
- `lucy fragment info` errors when database does not exist (does NOT silently create empty file)
- Fragment command group appears in `lucy --help` output
- Existing `lucy dereplicate c13` and `lucy predict c13` commands still work (no import errors or regressions)
</success_criteria>

<output>
After completion, create `.planning/phases/49-fragment-schema-infrastructure/49-02-SUMMARY.md`
</output>
