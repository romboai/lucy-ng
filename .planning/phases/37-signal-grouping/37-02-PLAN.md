---
phase: 37-signal-grouping
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_lsd_grouping_syntax.py
autonomous: true

must_haves:
  truths:
    - "LSD accepts HMBC (2 3) 8 syntax for 2-way combinatorial exchange"
    - "LSD accepts HMBC (2 3 4) 8 syntax for 3-way combinatorial exchange"
    - "LSD produces solutions when parenthesized atom lists are used in HMBC constraints"
    - "Documentation warns about false positive risk with close shifts"
  artifacts:
    - path: "tests/test_lsd_grouping_syntax.py"
      provides: "Validation tests for LSD parenthesized syntax"
      min_lines: 40
  key_links: []
---

<objective>
Validate that LSD parenthesized syntax `HMBC (2 3) 8` works correctly for combinatorial atom exchange by running actual LSD solver with test cases.

Purpose: Confirms that the output format from Plan 37-01's `lsd_atom_list()` is valid LSD input. The research flagged "LSD syntax validation" as MEDIUM confidence — this plan resolves that uncertainty before the CLI exposes this to users.

Output: Test file validating LSD accepts parenthesized atom lists and produces solutions.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-signal-grouping/37-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate LSD parenthesized syntax with test LSD files</name>
  <files>tests/test_lsd_grouping_syntax.py</files>
  <action>
    Create a test file that validates LSD parenthesized syntax by:

    1. Write a minimal LSD file with parenthesized HMBC constraints (e.g., `HMBC (2 3) 8`)
    2. Run LSD solver on it and verify it produces solutions (not an error)
    3. Test 2-way grouping: `HMBC (2 3) 8`
    4. Test 3-way grouping: `HMBC (2 3 4) 8`
    5. Compare solution count: parenthesized should produce >= solutions vs fixed assignment

    Use a simple test molecule (e.g., propanol C3H8O = 3 carbons + 1 oxygen) with known HMBC correlations.

    Use pytest fixtures to create temporary LSD files and run LSD via subprocess.

    Skip tests if LSD binary not found (use `pytest.mark.skipif` with `shutil.which("LSD")` check).

    The test validates the LSD MANUAL syntax claim that parenthesized lists enable combinatorial exploration. This is NOT testing the grouping algorithm (that's 37-01) — it's testing that LSD understands the output format.

    IMPORTANT: Check LSD availability with `shutil.which("LSD")`. If LSD is not available, skip tests gracefully with a clear message.
  </action>
  <verify>
    ```bash
    # Run LSD syntax tests (will skip if LSD not installed)
    pytest tests/test_lsd_grouping_syntax.py -v

    # Verify test file exists and has content
    wc -l tests/test_lsd_grouping_syntax.py
    ```
  </verify>
  <done>
    - Test file validates 2-way and 3-way parenthesized HMBC syntax with actual LSD runs
    - Tests skip gracefully if LSD not available
    - If LSD available: tests pass, confirming parenthesized syntax produces solutions
  </done>
</task>

</tasks>

<verification>
```bash
# Run tests
pytest tests/test_lsd_grouping_syntax.py -v

# Check for lint errors
ruff check tests/test_lsd_grouping_syntax.py
```
</verification>

<success_criteria>
- LSD parenthesized syntax `HMBC (2 3) 8` confirmed working or syntax issue documented
- Tests skip cleanly when LSD not installed
- No false assumptions about LSD syntax in grouping output format
</success_criteria>

<output>
After completion, create `.planning/phases/37-signal-grouping/37-02-SUMMARY.md`
</output>
