---
phase: 42-agent-definitions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/.claude/agents/lucy-nmr-chemist.md"
autonomous: true

must_haves:
  truths:
    - "NMR-Chemist agent has complete NMR background knowledge (experiment types, shift regions, pitfalls)"
    - "NMR-Chemist agent has statistical detection protocol (4 commands, interpretation, constraint translation)"
    - "NMR-Chemist agent has chemistry-first hierarchy for conflict resolution"
    - "NMR-Chemist agent has spectral quality assessment knowledge"
    - "NMR-Chemist agent has error tolerance and confidence scoring knowledge"
    - "NMR-Chemist agent has team communication instructions (SendMessage, TaskList, TaskUpdate)"
    - "NMR-Chemist agent has inter-agent message interface (outputs and inputs)"
  artifacts:
    - path: "~/.claude/agents/lucy-nmr-chemist.md"
      provides: "Full NMR-Chemist specialist agent definition"
      contains: "lucy detect hybridisation"
  key_links:
    - from: "~/.claude/agents/lucy-nmr-chemist.md"
      to: "~/.claude/commands/lucy-ng/case.md"
      via: "subagent_type in Task(team_name) spawn"
      pattern: "lucy-nmr-chemist"
---

<objective>
Replace the NMR-Chemist stub with a full specialist agent definition containing distributed domain knowledge from the monolithic lucy-case-agent.md.

Purpose: The NMR-Chemist is the team's spectral analysis expert. It handles peak picking, statistical detection, spectral quality assessment, and the chemistry-first evidence hierarchy. It must have enough inlined knowledge to operate autonomously without reading external files.

Output: Full agent definition at ~/.claude/agents/lucy-nmr-chemist.md (200-280 lines)
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/42-agent-definitions/42-CONTEXT.md
@.planning/phases/42-agent-definitions/42-RESEARCH.md

# Source: monolithic agent to decompose
@~/.claude/agents/lucy-case-agent.md

# Reference: existing stub to replace
@~/.claude/agents/lucy-nmr-chemist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create full NMR-Chemist agent definition</name>
  <files>~/.claude/agents/lucy-nmr-chemist.md</files>
  <action>
Replace the stub at ~/.claude/agents/lucy-nmr-chemist.md with the full NMR-Chemist specialist definition. Read the monolithic agent at ~/.claude/agents/lucy-case-agent.md as the knowledge source.

**YAML frontmatter:**
```yaml
---
name: lucy-nmr-chemist
description: >
  NMR spectral analysis specialist for CASE team. Handles peak picking,
  statistical detection, spectral quality assessment, and chemistry-first
  evidence hierarchy. Spawned by /lucy-ng:case orchestrator as team member.
tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
model: claude-opus-4-6
---
```

**Agent structure (use XML tags like the monolithic agent):**

1. `<role>` section (~15 lines):
   - Identity: NMR-Chemist specialist in CASE team
   - Spawned by: /lucy-ng:case orchestrator via Task(team_name)
   - Job: Analyze NMR spectra, pick peaks, run statistical detection, assess quality, assign multiplicities
   - ABSOLUTE PROHIBITION: NEVER write LSD files (that's lsd-engineer's job), NEVER rank solutions (that's solution-analyst's job), NEVER run lucy lsd run or outlsd
   - Team communication: Claim tasks from TaskList, report via SendMessage, mark completed via TaskUpdate

2. `<shared_context>` section (~8 lines):
   - Brief CASE team overview: 4 specialists + orchestrator as coordinator
   - What the workflow does: NMR data -> constraints -> LSD solver -> ranked structures
   - Where files live: analysis/ directory with iteration subfolders
   - What NMR experiments are available: 13C, DEPT-135, DEPT-90, HSQC, HMBC

3. `<domain_knowledge>` section (~180-220 lines) — COMPRESS from monolith:

   **Section 1: NMR Background** (compress from 176 to ~60 lines):
   - Experiment types table (keep verbatim — essential reference)
   - 13C chemical shift regions table (keep verbatim)
   - Pitfalls 1-9 COMPRESSED: Keep the rule/principle for each pitfall as a 2-3 line summary. Remove extended explanations and worked examples. Keep Pitfall 6 (heteroatom constraint strategy) and Pitfall 7 (H budget) at ~8 lines each since they're the most critical. Keep Pitfall 8 (over-trusting detection) and Pitfall 9 (under-using detection) at ~5 lines each.

   **Section 2: Spectral Quality** (compress from 50 to ~25 lines):
   - Keep S/N quality tiers table
   - Keep digital resolution tiers table
   - Keep artifact recognition (1J leakage) — compress to 3 lines
   - Remove "when to assess" prose (obvious)

   **Section 3.5: Statistical Detection Protocol** (compress from 233 to ~70 lines):
   - Keep overview with 4-command table
   - Keep selective detection strategy table (which shifts to query)
   - Keep CLI syntax for all 4 commands (verbatim — cannot paraphrase CLI)
   - Keep interpretation rules (mandatory >95%, forbidden <1%, ambiguous)
   - REMOVE constraint translation examples (lsd-engineer handles translation)
   - REMOVE documentation format (lsd-engineer writes CASE-PROGRESS.md)
   - Keep conflict documentation requirement (1 line: "Document every override in CASE-PROGRESS.md")

   **Section 3.6: Chemistry-First Hierarchy** (compress from 183 to ~30 lines):
   - Keep evidence priority table (6 rows)
   - Keep conflict resolution decision tree (the IF/THEN rules)
   - REMOVE 3 worked examples (agent can reason through conflicts using the decision tree)
   - Keep threshold override guidelines table
   - Keep closing principle as 2-line summary

   **Section 6: Error Tolerance** (compress from 85 to ~20 lines):
   - Keep close carbon detection formula and ambiguity criterion
   - Keep DEPT/HSQC multiplicity conflict priority tree (compressed)
   - Keep quaternary carbon HMBC sparsity table
   - REMOVE verbose resolution strategies

   **Section 7: Confidence Scoring** (compress from 60 to ~15 lines):
   - Keep 3-factor model (digital resolution, MAE, supporting correlations)
   - Keep downgrade rules (5 rules, 1 line each)
   - Keep per-structure confidence derivation (High/Medium/Low criteria)
   - Cross-reference: "See solution-analyst for ranking integration"

4. `<message_interface>` section (~15 lines):

   **OUTPUTS (what NMR-Chemist posts to team via SendMessage):**
   - Peak assignments: markdown table with columns Atom#, Shift, Multiplicity, Confidence
   - Statistical detection results: hybridisation, neighbours, hhb, grouping results per shift
   - Spectral quality assessment: S/N tier, resolution tier, artifact flags
   - Multiplicity analysis: CH3/CH2/CH/Cq assignments with DEPT evidence

   **INPUTS (what NMR-Chemist reads from other agents):**
   - From lsd-engineer: requests for additional detection or peak re-analysis
   - From devils-advocate: questions about multiplicity conflicts or detection overrides
   - From orchestrator: advisory constraints or task assignments

5. `<workflow>` section (~15 lines):
   - Step 1: Claim peak-picking task from TaskList
   - Step 2: Read NMR data paths from task description
   - Step 3: Assess spectral quality (S/N, resolution) for all spectra
   - Step 4: Pick peaks: lucy pick 1d (13C, DEPT), lucy pick hsqc, lucy pick hmbc
   - Step 5: Analyze symmetry: lucy analyze symmetry
   - Step 6: Run statistical detection (selective strategy per shift ranges)
   - Step 7: Compile results and post to team via SendMessage
   - Step 8: Mark task completed via TaskUpdate
   - Step 9: Monitor TaskList for additional requests

**Target: 200-280 lines total.** If exceeding 300 lines, compress further. If below 180, check for missing knowledge.

**Critical: Keep all CLI command syntax verbatim.** Compress prose, not commands. The agent must run exact CLI commands.
  </action>
  <verify>
Verify the file:

1. Check line count is in range:
```bash
wc -l ~/.claude/agents/lucy-nmr-chemist.md
```
Expected: 200-300 lines

2. Check required sections exist:
```bash
grep -c "role\|shared_context\|domain_knowledge\|message_interface\|workflow" ~/.claude/agents/lucy-nmr-chemist.md
```
Expected: 5+ matches (opening and closing tags)

3. Check critical knowledge is present:
```bash
grep -c "lucy detect hybridisation\|lucy detect neighbours\|lucy detect hhb\|lucy analyze grouping\|DEPT-135\|Chemistry-First\|confidence" ~/.claude/agents/lucy-nmr-chemist.md
```
Expected: 7+ matches (all 4 detection commands + DEPT + hierarchy + confidence)

4. Check prohibitions are present:
```bash
grep -c "NEVER write LSD\|NEVER rank\|NEVER run lucy lsd" ~/.claude/agents/lucy-nmr-chemist.md
```
Expected: 3 matches

5. Check team communication:
```bash
grep -c "SendMessage\|TaskList\|TaskUpdate" ~/.claude/agents/lucy-nmr-chemist.md
```
Expected: 3+ matches
  </verify>
  <done>Full NMR-Chemist agent definition exists at ~/.claude/agents/lucy-nmr-chemist.md with 200-280 lines, containing: NMR background (experiment types, shift regions, compressed pitfalls), spectral quality assessment, statistical detection protocol (4 CLI commands with interpretation), chemistry-first hierarchy, error tolerance, confidence scoring, team communication instructions, and inter-agent message interface.</done>
</task>

</tasks>

<verification>
- [ ] ~/.claude/agents/lucy-nmr-chemist.md exists and is 200-300 lines
- [ ] YAML frontmatter has name: lucy-nmr-chemist, model: claude-opus-4-6
- [ ] All 4 detection CLI commands present (hybridisation, neighbours, hhb, grouping)
- [ ] Chemistry-first hierarchy evidence priority table present
- [ ] Confidence scoring 3-factor model present
- [ ] NEVER write LSD / NEVER rank prohibitions present
- [ ] SendMessage/TaskList/TaskUpdate team communication present
- [ ] Message interface defines OUTPUTS and INPUTS
</verification>

<success_criteria>
NMR-Chemist agent has complete, self-contained NMR domain knowledge sufficient to perform peak picking, statistical detection, quality assessment, and multiplicity analysis without reading external files. Agent respects role boundaries (no LSD file writing, no ranking).
</success_criteria>

<output>
After completion, create `.planning/phases/42-agent-definitions/42-01-SUMMARY.md`
</output>
