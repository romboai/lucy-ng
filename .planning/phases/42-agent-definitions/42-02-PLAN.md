---
phase: 42-agent-definitions
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - "~/.claude/agents/lucy-lsd-engineer.md"
autonomous: true

must_haves:
  truths:
    - "LSD-Engineer agent has complete LSD command reference (MULT, HSQC, HMBC, BOND, LIST, PROP, ELEM, DEFF NOT, ELIM)"
    - "LSD-Engineer agent has incremental HMBC strategy (core principle, selection, adaptive loop, stopping, recovery)"
    - "LSD-Engineer agent has CASE workflow file organization rules"
    - "LSD-Engineer agent has full CASE-PROGRESS.md format specification"
    - "LSD-Engineer agent has correlation order rule and solution conversion (outlsd 5)"
    - "LSD-Engineer agent has team communication instructions"
    - "LSD-Engineer agent has badlist filters (DEFF NOT patterns for strained rings)"
  artifacts:
    - path: "~/.claude/agents/lucy-lsd-engineer.md"
      provides: "Full LSD-Engineer specialist agent definition"
      contains: "lucy lsd run"
  key_links:
    - from: "~/.claude/agents/lucy-lsd-engineer.md"
      to: "~/.claude/commands/lucy-ng/case.md"
      via: "subagent_type in Task(team_name) spawn"
      pattern: "lucy-lsd-engineer"
---

<objective>
Replace the LSD-Engineer stub with a full specialist agent definition containing distributed domain knowledge from the monolithic lucy-case-agent.md.

Purpose: The LSD-Engineer is the team's constraint building and solver expert. It handles LSD file construction, the incremental HMBC iteration strategy, constraint inventory management, and progress logging. It must have enough inlined knowledge to write correct LSD files and manage the iterative constraint-addition workflow autonomously.

Output: Full agent definition at ~/.claude/agents/lucy-lsd-engineer.md (220-300 lines)
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/42-agent-definitions/42-CONTEXT.md
@.planning/phases/42-agent-definitions/42-RESEARCH.md

# Source: monolithic agent to decompose
@~/.claude/agents/lucy-case-agent.md

# Reference: existing stub to replace
@~/.claude/agents/lucy-lsd-engineer.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create full LSD-Engineer agent definition</name>
  <files>~/.claude/agents/lucy-lsd-engineer.md</files>
  <action>
Replace the stub at ~/.claude/agents/lucy-lsd-engineer.md with the full LSD-Engineer specialist definition. Read the monolithic agent at ~/.claude/agents/lucy-case-agent.md as the knowledge source.

**YAML frontmatter:**
```yaml
---
name: lucy-lsd-engineer
description: >
  LSD constraint building and solver specialist for CASE team. Handles LSD file
  construction, incremental HMBC iteration, constraint inventory management,
  solution conversion, and progress logging. Spawned by /lucy-ng:case orchestrator.
tools:
  - Read
  - Write
  - Bash
  - Glob
  - Grep
model: claude-opus-4-6
---
```

**Agent structure:**

1. `<role>` section (~15 lines):
   - Identity: LSD-Engineer specialist in CASE team
   - Spawned by: /lucy-ng:case orchestrator via Task(team_name)
   - Job: Build LSD files from peak assignments, manage constraints, run LSD solver, follow incremental HMBC strategy, write CASE-PROGRESS.md entries
   - ABSOLUTE PROHIBITION: NEVER pick peaks (nmr-chemist's job), NEVER rank solutions (solution-analyst's job), NEVER skip writing CASE-PROGRESS.md after an LSD run
   - CRITICAL RULE: ALWAYS read the previous iteration's LSD file before writing a new one. NEVER reconstruct constraints from memory.
   - Team communication instructions

2. `<shared_context>` section (~8 lines):
   - Brief CASE team overview
   - Iteration workflow: detect -> build -> validate -> solve -> rank
   - File structure: analysis/iteration_NN/

3. `<domain_knowledge>` section (~180-240 lines) — from monolith Sections 3, 4, 5, 8:

   **Section 3: LSD Command Reference** (keep ~100 lines, nearly verbatim):
   - MULT command syntax with examples (element, hybridization, hydrogen count)
   - HSQC command syntax
   - HMBC command syntax (including parenthesized syntax for grouped signals)
   - BOND command syntax
   - LIST, ELEM, PROP command syntax with examples
   - SYME command syntax for signal equivalence
   - Correlation order rule: HSQC MUST appear before HMBC (keep verbatim — critical)
   - Hybridization rules: sp2 count must be EVEN (keep verbatim)
   - ELIM command: last resort, incremental (ELIM 1 0, then 2 0)
   - Solution conversion: `outlsd 5 < compound.sol > solutions.smi` (keep verbatim)
   - Solution count interpretation table (0, 1, 2-10, 10-100, >100)
   - Manual file construction checklist (8 items)
   - Badlist filters: ALL DEFF NOT patterns for strained rings (keep verbatim — these are the patterns that were dropped in v3.0 UAT)

   **Section 4: Incremental HMBC Strategy** (compress from 95 to ~50 lines):
   - Core principle: NEVER add all HMBC at once (keep verbatim)
   - High-confidence correlation selection criteria (4 criteria, compressed)
   - Adaptive iteration loop algorithm (keep the numbered algorithm)
   - Stopping conditions (success <=10, cap ~10, exhausted)
   - Zero-solution recovery (7-step diagnostic, compressed to numbered list)
   - Convergence stall detection (keep 3-iteration rule)
   - REMOVE "What NOT to Do" section (redundant with algorithm)

   **Section 5: File Organization** (keep ~25 lines, nearly verbatim):
   - Directory structure diagram
   - Rules: analysis/iteration_NN/, run LSD from iteration dir, .sol stays contained
   - Zero-padded iteration numbers

   **Section 8: CASE-PROGRESS.md Format** (keep ~40 lines — full specification):
   - Purpose, location, append-only rule
   - File structure template (header + Setup + Iteration N sections)
   - Required fields table (all 11 fields)
   - Format notes (constraints individually, "why" reasoning, sp2 check, H budget)
   - THIS is the authoritative CASE-PROGRESS.md specification (other agents get abbreviated version)

4. `<message_interface>` section (~15 lines):

   **OUTPUTS (what LSD-Engineer posts to team via SendMessage):**
   - Iteration summary: constraints added/removed, solution count, LSD file path
   - Request for additional HMBC correlations or detection results (to nmr-chemist)
   - Ready-for-validation notification (to devils-advocate, before LSD run)
   - CASE-PROGRESS.md update notification (to all)

   **INPUTS (what LSD-Engineer reads from other agents):**
   - From nmr-chemist: peak assignments, detection results, multiplicity analysis
   - From devils-advocate: validation approval or issue flags
   - From solution-analyst: ranking results triggering next iteration decisions
   - From orchestrator: advisory constraints, task assignments

5. `<workflow>` section (~20 lines):
   - Step 1: Claim LSD iteration task from TaskList
   - Step 2: Read peak assignments from nmr-chemist's message
   - Step 3: If iteration > 1: Read previous iteration's LSD file (NEVER reconstruct from memory)
   - Step 4: Build/update LSD file incorporating: MULT defs, HSQC, HMBC batch, BOND/LIST/PROP, DEFF NOT badlist
   - Step 5: Write LSD file to analysis/iteration_NN/compound.lsd
   - Step 6: Post ready-for-validation to devils-advocate via SendMessage
   - Step 7: WAIT for devils-advocate approval before running LSD
   - Step 8: Run LSD: `cd analysis/iteration_NN && lucy lsd run compound.lsd`
   - Step 9: IMMEDIATELY write CASE-PROGRESS.md entry (before anything else)
   - Step 10: If solutions <= 10: convert with `outlsd 5 < compound.sol > solutions.smi`
   - Step 11: Post iteration summary to team via SendMessage
   - Step 12: Mark task completed via TaskUpdate

**Target: 220-300 lines total.** LSD command syntax MUST be verbatim. Compress prose, not commands or algorithms.
  </action>
  <verify>
Verify the file:

1. Check line count:
```bash
wc -l ~/.claude/agents/lucy-lsd-engineer.md
```
Expected: 220-320 lines

2. Check critical LSD commands present:
```bash
grep -c "MULT\|HSQC\|HMBC\|BOND\|LIST.*ELEM.*PROP\|DEFF NOT\|ELIM\|outlsd 5\|SYME" ~/.claude/agents/lucy-lsd-engineer.md
```
Expected: 9+ matches

3. Check incremental HMBC strategy:
```bash
grep -c "incremental\|batch\|solution_count\|Zero-Solution\|Convergence Stall" ~/.claude/agents/lucy-lsd-engineer.md
```
Expected: 5+ matches

4. Check CASE-PROGRESS.md format:
```bash
grep -c "CASE-PROGRESS\|append-only\|Iteration\|Solution count\|Constraints added" ~/.claude/agents/lucy-lsd-engineer.md
```
Expected: 5+ matches

5. Check file organization:
```bash
grep -c "iteration_01\|iteration_NN\|analysis/" ~/.claude/agents/lucy-lsd-engineer.md
```
Expected: 3+ matches

6. Check prohibitions and critical rules:
```bash
grep -c "NEVER pick peaks\|NEVER rank\|NEVER reconstruct from memory\|read the previous" ~/.claude/agents/lucy-lsd-engineer.md
```
Expected: 3+ matches
  </verify>
  <done>Full LSD-Engineer agent definition exists at ~/.claude/agents/lucy-lsd-engineer.md with 220-300 lines, containing: complete LSD command reference (all commands), incremental HMBC strategy, file organization rules, full CASE-PROGRESS.md format specification, badlist filters, team communication, and message interface. Agent reads previous LSD file (never reconstructs) and waits for devils-advocate approval before running solver.</done>
</task>

</tasks>

<verification>
- [ ] ~/.claude/agents/lucy-lsd-engineer.md exists and is 220-320 lines
- [ ] YAML frontmatter has name: lucy-lsd-engineer, model: claude-opus-4-6
- [ ] All LSD commands present: MULT, HSQC, HMBC, BOND, LIST, ELEM, PROP, DEFF NOT, ELIM, SYME
- [ ] Correlation order rule present (HSQC before HMBC)
- [ ] Hybridization rule present (sp2 count even)
- [ ] outlsd 5 conversion present
- [ ] Incremental HMBC strategy with adaptive loop present
- [ ] CASE-PROGRESS.md full format specification present
- [ ] File organization with iteration_NN structure present
- [ ] Badlist filters (all DEFF NOT patterns) present
- [ ] "Read previous LSD file, never reconstruct" rule present
</verification>

<success_criteria>
LSD-Engineer agent has complete, self-contained LSD domain knowledge sufficient to build correct LSD files, manage the incremental HMBC iteration workflow, and write CASE-PROGRESS.md entries without reading external files. Agent respects role boundaries (no peak picking, no ranking) and always reads the previous iteration's file.
</success_criteria>

<output>
After completion, create `.planning/phases/42-agent-definitions/42-02-SUMMARY.md`
</output>
