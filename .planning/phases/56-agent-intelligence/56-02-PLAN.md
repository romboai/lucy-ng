---
phase: 56-agent-intelligence
plan: 02
type: execute
wave: 2
depends_on: [56-01]
files_modified:
  - ~/.claude/agents/lucy-lsd-engineer.md
  - ~/.claude/agents/lucy-solution-analyst.md
autonomous: true
requirements: [INTL-02, INTL-03]

must_haves:
  truths:
    - "lsd-engineer defers HMBC correlations flagged as potential 4J to the LAST HMBC batch, and skips them entirely if solutions already exist (<=10) before that batch"
    - "solution-analyst runs lucy predict c13 on top candidates to verify aromatic carbon presence via predicted shifts in 110-160 ppm range, not just the warnings array"
  artifacts:
    - path: "~/.claude/agents/lucy-lsd-engineer.md"
      provides: "4J deferral logic in HMBC batch selection"
      contains: "4J"
    - path: "~/.claude/agents/lucy-solution-analyst.md"
      provides: "Aromatic verification via 13C prediction"
      contains: "predict c13"
  key_links:
    - from: "~/.claude/agents/lucy-lsd-engineer.md"
      to: "nmr-chemist [SETUP-COMPLETE]"
      via: "Reading 'Potential 4J correlations' field to exclude from early HMBC batches"
      pattern: "Potential 4J"
    - from: "~/.claude/agents/lucy-solution-analyst.md"
      to: "lucy predict c13 CLI"
      via: "Counting predicted shifts in 110-160 ppm range for aromatic verification"
      pattern: "predict c13.*aromatic"
---

<objective>
Add 4J deferral to lsd-engineer and aromatic structural verification to solution-analyst.

Purpose: lsd-engineer must defer correlations flagged as potential 4J by nmr-chemist so they are only added as last resort, preventing wrong solutions from excluding the correct structure. solution-analyst must independently verify aromatic ring presence using 13C prediction rather than relying on the warnings array alone.

Output: Updated lucy-lsd-engineer.md with 4J deferral rules and updated lucy-solution-analyst.md with prediction-based aromatic verification.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/milestones/v6.0-ROADMAP.md
@.planning/STATE.md
@.planning/phases/56-agent-intelligence/56-01-PLAN.md

<interfaces>
<!-- From Plan 01: nmr-chemist [SETUP-COMPLETE] now includes: -->
<!-- Potential 4J correlations: -->
<!--   - C{N}({shift} ArCH) <-> H-C{M}({shift} {mult}): potential 4J through aromatic ring -->
<!--   - ... (or "None detected") -->

<!-- lsd-engineer reads [SETUP-COMPLETE] for peak assignments and detection results -->
<!-- lsd-engineer selects HMBC batches per Section 2 "Incremental HMBC Strategy" -->
<!-- solution-analyst already has Check 6 (Aromatic ring verification) using warnings array -->
<!-- solution-analyst already runs lucy predict c13 for top 3-5 candidates (workflow step 4) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add 4J deferral logic to lsd-engineer</name>
  <files>~/.claude/agents/lucy-lsd-engineer.md</files>
  <action>
Read ~/.claude/agents/lucy-lsd-engineer.md. Make these specific additions:

**A. Add new subsection "2.1 4J Deferral Rule" inside Section 2 (Incremental HMBC Strategy), after the "Selection Criteria" subsection (after line ~193):**

```
### 4J Deferral Rule

When nmr-chemist flags HMBC correlations as "Potential 4J" in [SETUP-COMPLETE]:

1. **NEVER include flagged 4J correlations in early HMBC batches** (iterations 1 through N-1)
2. **Defer them to the LAST batch** — only add after all non-flagged correlations are used or solution count is still >10
3. **Skip entirely if solutions already exist**: If solution_count <= 10 BEFORE reaching the deferred batch, do NOT add 4J correlations. Proceed directly to ranking.
4. Track deferred correlations in the constraint inventory under a new field `deferred_4j` (array of correlation strings from nmr-chemist's flagging)

**Why:** 4J couplings through aromatic rings satisfy LSD's 2-3 bond requirement numerically but exclude the correct aromatic structure. Wrong ring-size isomers survive instead. Adding them only as last resort (and skipping if unnecessary) prevents this trap.

**Inventory field:** Add `"deferred_4j": ["C4 H8", "C6 H9", ...]` to the constraint inventory JSON (copy from nmr-chemist's flagged list). When a deferred correlation is added in a later batch, move it from `deferred_4j` to the appropriate `hmbc_batches` entry. When skipped, leave in `deferred_4j` with a note in [ITERATION-COMPLETE]: "4J correlations skipped — solutions already converged."
```

**B. Update "Adaptive Loop" in Section 2 (around line 197-208):**

After step 1 ("Start with MULT + HSQC + ..."), add clarification:
```
   1a. EXCLUDE any HMBC correlations flagged as potential 4J by nmr-chemist from ALL early batches
```

After the existing stopping conditions (around line 212-216), add:
```
### 4J Batch (Final)

If all non-4J HMBC are exhausted AND solution_count > 10:
- Add deferred 4J correlations as the final batch
- If solutions drop to <=10: rank (but note "includes 4J correlations" caveat in [ITERATION-COMPLETE])
- If solutions go to 0: remove 4J batch immediately — confirms they are genuine 4J couplings

If solution_count <= 10 before reaching 4J batch: SKIP 4J correlations entirely. Report: "4J correlations skipped — solutions converged without them."
```

**C. Update Constraint Inventory JSON schema in Section 5A (around line 299-316):**

Add to the schema reference table:
```
| `deferred_4j` | string array | HMBC correlations flagged as potential 4J, deferred from early batches |
```

**D. Update [ITERATION-COMPLETE] message template in Section 4 (around line 262-281):**

Add a new field after "Confidence:":
```
4J status: <"N deferred (not yet added)" / "added as final batch" / "skipped — solutions converged" / "N/A — none flagged">
```

**E. Update workflow step 2 (around line 419-420):**

After "Read peak assignments from nmr-chemist's message", add:
```
2a. Extract "Potential 4J correlations" list from nmr-chemist's [SETUP-COMPLETE]. Store as deferred_4j in constraint inventory. These correlations MUST NOT appear in HMBC batches until all other correlations are used.
```
  </action>
  <verify>
    <automated>grep -c "4J\|deferred_4j" ~/.claude/agents/lucy-lsd-engineer.md | awk '{if ($1 >= 8) print "PASS: 4J deferral content found ("$1" occurrences)"; else print "FAIL: insufficient 4J deferral content ("$1" occurrences)"}'</automated>
  </verify>
  <done>lsd-engineer agent definition includes: (1) 4J Deferral Rule subsection with clear never/defer/skip logic, (2) deferred_4j field in constraint inventory schema, (3) 4J status field in [ITERATION-COMPLETE] template, (4) workflow step to extract 4J flags from nmr-chemist. At least 8 references to "4J" or "deferred_4j" in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Add prediction-based aromatic verification to solution-analyst</name>
  <files>~/.claude/agents/lucy-solution-analyst.md</files>
  <action>
Read ~/.claude/agents/lucy-solution-analyst.md. Make these specific additions:

**A. Update Check 6 (Aromatic ring verification) in Section 4 (around line 111):**

Replace the current Check 6 content with an enhanced version that adds prediction-based verification:

```
**Check 6 -- Aromatic ring verification (two-tier):**

**Tier 1 (warnings array):** Parse `warnings` from `lucy lsd rank --format json` output. If the `warnings` array contains an aromatic mismatch warning (text contains "Aromatic ring expected"), flag ALL solutions where `has_aromatic_ring` is `false` as QUESTIONABLE.

**Tier 2 (prediction-based, NEW):** For the top 3 ranked candidates, check the `lucy predict c13` output (already run in workflow step 4) for aromatic carbon evidence:
- Count predicted shifts in 110-160 ppm range
- If the experimental spectrum has 4+ carbons in 110-160 ppm BUT a candidate's predicted shifts show 0 carbons in that range: flag as STRUCTURALLY INCONSISTENT (stronger than QUESTIONABLE)
- If a candidate's predicted shifts show aromatic carbons matching the experimental count: increase confidence
- This catches cases where the warnings array alone is insufficient (e.g., when LSD produces structures that technically have aromatic atoms but in wrong positions)

**Root cause note:** This issue stems from 4J HMBC couplings through aromatic rings. When all solutions lack aromatic rings despite clear aromatic NMR evidence, the most likely fix is to identify and remove the offending HMBC correlations (they are 4-bond, not 2-3 bond). Report this remediation in the recommendation.

**Remediation to report:** "Identify HMBC correlations between aromatic ring carbon positions (110-160 ppm) and benzylic/alpha-substituent positions (0-55 ppm). These are likely 4J couplings through the aromatic ring. Remove the most suspect ones and re-run LSD."
```

**B. Update workflow step 4 (around line 222):**

Expand step 4 to make the aromatic check explicit:
```
4. For top 3-5 candidates: `lucy predict c13 "<SMILES>" --format json`
   4a. For each prediction result, count predicted shifts in 110-160 ppm range.
       Compare against experimental spectrum aromatic carbon count (from nmr-chemist's [SETUP-COMPLETE] "Aromatic expectation" field).
       Flag structural inconsistency if candidate lacks aromatic carbons that experimental data expects.
```

**C. Update [RANKING-COMPLETE] message template (around line 189-198):**

Add a new field after "Aromatic warning:":
```
Aromatic verification: <"CONFIRMED — top N candidates show M aromatic carbons (predicted)" / "INCONSISTENT — top N candidates lack aromatic carbons despite M experimental shifts in 110-160 ppm" / "N/A — no aromatic expectation">
```
  </action>
  <verify>
    <automated>grep -c "Tier 2\|STRUCTURALLY INCONSISTENT\|Aromatic verification\|predict c13.*aromatic\|110-160" ~/.claude/agents/lucy-solution-analyst.md | awk '{if ($1 >= 4) print "PASS: aromatic verification content found ("$1" occurrences)"; else print "FAIL: insufficient aromatic verification content ("$1" occurrences)"}'</automated>
  </verify>
  <done>solution-analyst agent definition includes: (1) Two-tier Check 6 with prediction-based aromatic verification (Tier 2), (2) STRUCTURALLY INCONSISTENT flag for candidates missing aromatic carbons, (3) "Aromatic verification" field in [RANKING-COMPLETE] template, (4) workflow step 4a counting predicted shifts in 110-160 ppm. At least 4 references to the new verification logic.</done>
</task>

</tasks>

<verification>
1. `grep "4J" ~/.claude/agents/lucy-lsd-engineer.md` — shows deferral rule, inventory field, iteration-complete field
2. `grep "deferred_4j" ~/.claude/agents/lucy-lsd-engineer.md` — shows inventory integration
3. `grep "Tier 2" ~/.claude/agents/lucy-solution-analyst.md` — shows prediction-based verification
4. `grep "STRUCTURALLY INCONSISTENT" ~/.claude/agents/lucy-solution-analyst.md` — shows enhanced flagging
5. `grep "Aromatic verification" ~/.claude/agents/lucy-solution-analyst.md` — shows new RANKING-COMPLETE field
6. `grep "predict c13" ~/.claude/agents/lucy-solution-analyst.md` — appears in verification context (not just ranking)
</verification>

<success_criteria>
- lsd-engineer defers 4J-flagged correlations to last HMBC batch
- lsd-engineer skips 4J correlations entirely when solutions already converge
- solution-analyst verifies aromatic ring presence via predicted shifts (not just warnings array)
- solution-analyst flags STRUCTURALLY INCONSISTENT when prediction contradicts experimental aromatic evidence
- Both agents' message templates include new 4J-related fields
- Constraint inventory schema includes deferred_4j field
</success_criteria>

<output>
After completion, create `.planning/phases/56-agent-intelligence/56-02-SUMMARY.md`
</output>
