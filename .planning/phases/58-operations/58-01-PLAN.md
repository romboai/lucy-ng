---
phase: 58-operations
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ~/.claude/commands/lucy-ng/status.md
  - ~/.claude/commands/lucy-ng/case.md
autonomous: true
requirements: [OPER-01, OPER-02]

must_haves:
  truths:
    - "status.md checks lucy --version output against a minimum required version and warns when below"
    - "case.md accepts a --smoke-test argument that runs exactly 1 iteration then stops with pass/fail"
    - "Smoke test output is clearly distinguished from normal CASE output via SMOKE TEST header"
    - "Smoke test verifies team spawn, peak picking, LSD file creation, and DA validation"
  artifacts:
    - path: "~/.claude/commands/lucy-ng/status.md"
      provides: "Version compatibility check in check_lucy step"
      contains: "MINIMUM_REQUIRED_VERSION"
    - path: "~/.claude/commands/lucy-ng/case.md"
      provides: "Smoke test mode with 1-iteration cap and structured pass/fail"
      contains: "smoke-test"
  key_links:
    - from: "~/.claude/commands/lucy-ng/status.md"
      to: "lucy --version"
      via: "version string parsing and semver comparison"
      pattern: "MINIMUM_REQUIRED_VERSION"
    - from: "~/.claude/commands/lucy-ng/case.md"
      to: "monitor_progress step"
      via: "smoke test early exit after iteration 1"
      pattern: "smoke.test"
---

<objective>
Add version compatibility checking to the status skill and a smoke test mode to the CASE orchestrator skill.

Purpose: Catch CLI version mismatches before workflows fail, and provide a fast 1-iteration validation path for the CASE pipeline without running to convergence.
Output: Updated status.md with version check, updated case.md with --smoke-test mode.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/milestones/v6.0-ROADMAP.md
@.planning/STATE.md
@~/.claude/commands/lucy-ng/status.md
@~/.claude/commands/lucy-ng/case.md

<interfaces>
<!-- status.md current structure: 4 steps (check_lucy, check_lsd, check_database, report) -->
<!-- case.md current structure: steps parse_arguments, validate_prerequisites, spawn_case_team, write_progress, validate_message, monitor_progress, detect_loops, diagnose, intervene, track_and_decide, escalate, deliver_advisory_and_results, terminate_team -->

From status.md check_lucy step (line 17-21):
```
Run `lucy --version` via Bash.
- If successful: record version string
- If command not found: record status as MISSING, action: "Run: pip install lucy-ng"
```

From case.md parse_arguments step (line 31-54):
```
Extract compound path and molecular formula from user input.
Arguments: <compound_path> <formula> -- both optional if inferable.
```

From lucy --version output format:
```
lucy, version 0.1.0
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add version compatibility check to status.md</name>
  <files>~/.claude/commands/lucy-ng/status.md</files>
  <action>
Edit the status.md skill to add version compatibility checking in the check_lucy step.

1. After the existing `lucy --version` check that records the version string, add version comparison logic:

   Define minimum required version as `0.1.0` (current release). This constant should be clearly visible at the top of the check_lucy step so it is easy to update.

   Parse the version from `lucy --version` output (format: `lucy, version X.Y.Z`) — extract X.Y.Z.

   Compare installed version against minimum using semver logic:
   - Split both versions on `.` into [major, minor, patch]
   - Compare major first, then minor, then patch
   - If installed < minimum: record status as INCOMPATIBLE with action text:
     ```
     Installed lucy-ng version X.Y.Z is below minimum required Y.Y.Y.
     Run: pip install --upgrade lucy-ng
     ```

2. Update the report step's status table to show INCOMPATIBLE as a third possible status (alongside OK and MISSING):

   Add a row note: if INCOMPATIBLE, the status column shows "INCOMPATIBLE" and Details shows the version mismatch message.

3. Add a new line after the "If all OK" conditional in the report step:
   ```
   If any INCOMPATIBLE: "Version mismatch detected. Upgrade before running workflows."
   ```

Keep the edit minimal — do not restructure the existing steps or change any other behavior.
  </action>
  <verify>
    <automated>grep -c "MINIMUM_REQUIRED_VERSION\|INCOMPATIBLE\|minimum required" ~/.claude/commands/lucy-ng/status.md</automated>
  </verify>
  <done>status.md check_lucy step parses version, compares against minimum, and report step shows INCOMPATIBLE status with upgrade instruction when version is below minimum</done>
</task>

<task type="auto">
  <name>Task 2: Add smoke test mode to case.md</name>
  <files>~/.claude/commands/lucy-ng/case.md</files>
  <action>
Edit case.md to add a smoke test mode that runs exactly 1 CASE iteration and reports pass/fail.

1. **Update parse_arguments step** — add `--smoke-test` flag detection:

   After the existing argument parsing, add:
   ```
   **Smoke test mode:** If the user's invocation includes `--smoke-test` or `smoke test`:
   - Set SMOKE_TEST = true
   - Default compound_path to `data/Ibuprofen` if not provided (well-known test dataset)
   - Default formula to `C13H18O2` if not provided (ibuprofen's formula)
   - Print header:
     ```
     ========================================
     SMOKE TEST — CASE Pipeline Validation
     ========================================
     Compound: {compound_path}
     Formula: {formula}
     Mode: 1-iteration validation (not full CASE)
     ```
   ```

2. **Update monitor_progress step** — add smoke test exit after iteration 1:

   After the existing "On each structured message" monitoring logic, before the "Completion signals" section, add:

   ```
   **Smoke test early exit:**
   If SMOKE_TEST is true, track these checkpoints as they arrive:
   - [ ] [SETUP-COMPLETE] received from nmr-chemist (peak picking done)
   - [ ] [ITERATION-COMPLETE] received from lsd-engineer (LSD file built and run)
   - [ ] [VALIDATION-PASSED] or [VALIDATION-BLOCKED] received from devils-advocate

   When all 3 checkpoints are complete (or any checkpoint fails with error/timeout after 5 minutes):
   - Do NOT create further iteration tasks
   - Do NOT proceed to ranking
   - Proceed directly to smoke_test_report step
   ```

3. **Add new step `smoke_test_report`** — insert before the `terminate_team` step:

   ```xml
   <step name="smoke_test_report">
   Generate smoke test pass/fail report. Only runs when SMOKE_TEST is true.

   **Determine result for each checkpoint:**
   - PASS: message received with all required fields validated
   - FAIL: message not received, or validate_message rejected it, or agent error

   **Print report:**
   ```
   ========================================
   SMOKE TEST RESULTS
   ========================================

   | Checkpoint              | Status | Details          |
   |-------------------------|--------|------------------|
   | Team spawned            | PASS/FAIL | 4 agents created |
   | NMR-chemist peak pick   | PASS/FAIL | [SETUP-COMPLETE] |
   | LSD-engineer build file | PASS/FAIL | [ITERATION-COMPLETE] |
   | Devils-advocate validate| PASS/FAIL | [VALIDATION-*]   |

   Overall: PASS / FAIL (N/4 checkpoints passed)
   ```

   **After report:** Proceed to terminate_team step (normal team cleanup).
   </step>
   ```

4. **Update terminate_team step** — add a note at the top:
   ```
   **This step executes after:** present_results (successful or failed CASE), OR escalate (user escalation), OR smoke_test_report (smoke test mode).
   ```

Keep all existing case.md content intact. The smoke test mode adds a parallel code path that exits early — it does not change the normal CASE flow.
  </action>
  <verify>
    <automated>grep -c "smoke.test\|SMOKE.TEST\|smoke_test_report\|SMOKE TEST" ~/.claude/commands/lucy-ng/case.md</automated>
  </verify>
  <done>case.md accepts --smoke-test flag, defaults to Ibuprofen data, runs 1 iteration tracking 3 checkpoints (setup-complete, iteration-complete, validation), prints structured SMOKE TEST RESULTS table with pass/fail per checkpoint, then terminates team</done>
</task>

</tasks>

<verification>
1. `grep "MINIMUM_REQUIRED_VERSION\|INCOMPATIBLE" ~/.claude/commands/lucy-ng/status.md` returns matches for version checking
2. `grep "smoke.test\|SMOKE TEST" ~/.claude/commands/lucy-ng/case.md` returns matches for smoke test mode
3. `wc -l ~/.claude/commands/lucy-ng/case.md` shows case.md grew by ~60-80 lines (not a major bloat)
4. `wc -l ~/.claude/commands/lucy-ng/status.md` shows status.md grew by ~15-20 lines
</verification>

<success_criteria>
- status.md has version comparison logic with MINIMUM_REQUIRED_VERSION = 0.1.0
- status.md report step shows INCOMPATIBLE status when version is below minimum
- case.md parse_arguments detects --smoke-test flag and defaults to Ibuprofen data
- case.md monitor_progress exits after 1 iteration in smoke test mode
- case.md has smoke_test_report step with structured pass/fail table
- Smoke test output has distinct "SMOKE TEST" header distinguishing it from normal CASE
- Normal CASE workflow is unchanged (smoke test is a parallel path)
</success_criteria>

<output>
After completion, create `.planning/phases/58-operations/58-01-SUMMARY.md`
</output>
