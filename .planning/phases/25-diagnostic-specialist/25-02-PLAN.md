---
phase: 25-diagnostic-specialist
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .claude/agents/supervisor.md
  - skill/supervisor/SKILL.md
autonomous: true

must_haves:
  truths:
    - "Supervisor knows when to spawn diagnostic specialist (after 2 failed interventions with same pattern OR when basic checks pass but CASE agent still stuck)"
    - "Supervisor spawns diagnostic specialist via Task tool with compound path, LSD file, CASE-PROGRESS.md, and failure type"
    - "Supervisor reads DIAGNOSTIC-REPORT.md after specialist completes and uses root cause + fixes to advise CASE agent"
    - "Supervisor does NOT spawn diagnostic specialist for routine iterations (basic diagnosis is sufficient)"
  artifacts:
    - path: ".claude/agents/supervisor.md"
      provides: "Updated supervisor agent with diagnostic specialist delegation section"
      contains: "diagnostic-specialist"
    - path: "skill/supervisor/SKILL.md"
      provides: "Updated supervisor skill with diagnostic specialist delegation criteria and workflow"
      contains: "diagnostic-specialist"
  key_links:
    - from: ".claude/agents/supervisor.md"
      to: ".claude/agents/diagnostic-specialist.md"
      via: "Task tool agent_type reference"
      pattern: "diagnostic-specialist"
    - from: "skill/supervisor/SKILL.md"
      to: "skill/diagnostic/SKILL.md"
      via: "cross-reference for diagnostic procedures"
      pattern: "skill/diagnostic/SKILL\\.md"
---

<objective>
Update the supervisor agent and its skill document to integrate diagnostic specialist delegation -- when to spawn the specialist, what to send it, and how to use its report.

Purpose: The supervisor (Phase 24) currently does basic diagnosis itself. This plan adds the escalation path where the supervisor delegates to the diagnostic specialist (Phase 25) for deep root cause analysis when basic diagnosis is insufficient.

Output: Updated .claude/agents/supervisor.md and skill/supervisor/SKILL.md with diagnostic specialist integration.
</objective>

<execution_context>
@/Users/steinbeck/.claude/get-shit-done/workflows/execute-plan.md
@/Users/steinbeck/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-diagnostic-specialist/25-RESEARCH.md
@.claude/agents/supervisor.md
@skill/supervisor/SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update skill/supervisor/SKILL.md with diagnostic specialist delegation</name>
  <files>skill/supervisor/SKILL.md</files>
  <action>
Add a new section to skill/supervisor/SKILL.md for diagnostic specialist delegation. Insert as Section 4.5 (after loop detection patterns in Section 4 but before convergence criteria in Section 5), OR as a new Section 8 at the end before the cross-references line. Choose whichever placement creates better flow.

**Content to add (~80-120 lines):**

**Diagnostic Specialist Delegation**

When basic supervisor diagnosis (Section 4 procedures) is insufficient, delegate to the diagnostic specialist for deep root cause analysis.

**When to delegate (threshold criteria):**
- After 2 failed supervisor interventions with the SAME loop pattern (i.e., supervisor diagnosed, advised CASE agent, but pattern recurred twice)
- When ALL basic checks pass (sp2 count even, H budget correct, no obvious 1J artifacts) but CASE agent is still stuck with 0 solutions or 1000+ solutions
- When constraint churning (SUPV-05) persists after reset to known-good state

**When NOT to delegate:**
- Routine iterations (CASE agent progressing normally)
- First detection of a loop pattern (supervisor basic diagnosis is sufficient)
- When root cause is obvious from basic checks (e.g., odd sp2 count -- no specialist needed)

**How to spawn diagnostic specialist:**
Provide a Task tool invocation template:
```
Task(
  agent_type="diagnostic-specialist",
  instructions="Analyze LSD failure for compound at <compound_path>.

  Read:
  - <compound_path>/CASE-PROGRESS.md (iteration history)
  - <compound_path>/<filename>.lsd (latest LSD file)

  Failure type: <0 solutions | 1000+ solutions>

  Run systematic diagnostic checks per skill/diagnostic/SKILL.md.
  Document ALL checks (PASS and FAIL).
  Identify root cause with evidence.

  Write structured report to <compound_path>/DIAGNOSTIC-REPORT.md.
  Include: findings, root cause, recommended fixes with LSD command examples.
  Rate all findings and recommendations as HIGH/MEDIUM/LOW confidence.
  "
)
```

**After diagnostic specialist completes:**
1. Read `<compound_path>/DIAGNOSTIC-REPORT.md`
2. Extract root cause from "## Root Cause" section
3. Extract primary fix from "## Recommended Fixes" section (Fix marked PRIMARY)
4. Formulate advisory for CASE agent that incorporates the diagnostic findings:
   - Reference the diagnostic report: "See DIAGNOSTIC-REPORT.md for full analysis"
   - Include the specific fix action with LSD command examples from the report
   - Include verification steps from the report
5. Re-spawn CASE agent with the diagnostic-informed advisory

**DIAGNOSTIC-REPORT.md retention:**
- Each diagnostic overwrites the previous DIAGNOSTIC-REPORT.md (single file, latest diagnostic only)
- CASE-PROGRESS.md tracks diagnostic history via iteration notes: "Diagnostic specialist invoked, see DIAGNOSTIC-REPORT.md"
- If history is needed, supervisor references CASE-PROGRESS.md notes

**Escalation after diagnostic specialist:**
If diagnostic specialist's recommended fix is applied but problem persists:
- This counts toward the per-pattern 10-cycle escalation limit (Section 6)
- After 10 total intervention cycles (basic + specialist-informed), escalate to user
- Include diagnostic report reference in escalation: "Diagnostic specialist analysis available at DIAGNOSTIC-REPORT.md"

Also update the existing Section 1 note that currently says "Phase 25 will add a diagnostic specialist" -- change it to state that the diagnostic specialist is available and reference the new section.

Cross-reference skill/diagnostic/SKILL.md for the specialist's domain knowledge (do NOT duplicate diagnostic procedures here).
  </action>
  <verify>
```bash
grep "diagnostic-specialist" skill/supervisor/SKILL.md  # Should find multiple references
grep "DIAGNOSTIC-REPORT.md" skill/supervisor/SKILL.md  # Report handling documented
grep "2 failed" skill/supervisor/SKILL.md  # Delegation threshold documented
grep "Task(" skill/supervisor/SKILL.md  # Task tool invocation template
grep "skill/diagnostic/SKILL.md" skill/supervisor/SKILL.md  # Cross-reference present
wc -l skill/supervisor/SKILL.md  # Should be ~760-800 lines (was 678)
```
  </verify>
  <done>
skill/supervisor/SKILL.md updated with diagnostic specialist delegation section containing: delegation criteria (2 failed interventions threshold), Task tool invocation template, post-diagnostic workflow (read report, advise CASE agent), report retention policy, escalation rules. Phase 25 placeholder note updated. Cross-references skill/diagnostic/SKILL.md without duplicating procedures.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update .claude/agents/supervisor.md with diagnostic specialist spawning</name>
  <files>.claude/agents/supervisor.md</files>
  <action>
Update the supervisor agent definition at .claude/agents/supervisor.md to include diagnostic specialist delegation. This is the agent's system prompt -- it needs to know WHEN and HOW to spawn the specialist.

**Changes to make:**

1. **Update description in YAML frontmatter** to mention diagnostic specialist:
   Change the description to include: "Delegates to diagnostic specialist for deep LSD root cause analysis when basic diagnosis is insufficient."

2. **Add a new section "Spawning the Diagnostic Specialist"** after the "Monitoring CASE Progress" section and before the "Loop Detection Patterns" section. Content (~40-60 lines):

   - **When to spawn**: After 2 failed interventions with same pattern, OR when basic checks pass but CASE agent still stuck
   - **When NOT to spawn**: Routine iterations, first loop detection, obvious root causes
   - **How to spawn**: Task tool invocation with agent_type="diagnostic-specialist", providing compound path, LSD file, CASE-PROGRESS.md path, failure type
   - **After specialist completes**: Read DIAGNOSTIC-REPORT.md, extract root cause and primary fix, formulate diagnostic-informed advisory for CASE agent
   - **Reference**: "For full delegation criteria and workflow, see skill/supervisor/SKILL.md Section [N]"

3. **Update the "Monitoring CASE Progress" section step 4** to add a sub-step:
   Current: "If loop detected: Diagnose the root cause..."
   Add: "If basic diagnosis insufficient (2+ failed interventions): Spawn diagnostic specialist (see section below)"

4. **Update the "Domain Knowledge References" section** to add:
   - "Diagnostic specialist domain knowledge (LSD manual, check procedures, report format): the diagnostic specialist references `skill/diagnostic/SKILL.md`"

5. **Update "Important Rules"** to add:
   - "NEVER spawn diagnostic specialist for routine iterations -- basic diagnosis is sufficient for first-time loop detection"

6. **Remove or update the Phase 24 note** in skill reference if any mention of "Phase 25 will add..." exists. The specialist is now available.

Keep changes surgical -- do NOT rewrite existing sections. Add the new section and update existing text minimally.
  </action>
  <verify>
```bash
grep "diagnostic-specialist" .claude/agents/supervisor.md  # Should find multiple references
grep "DIAGNOSTIC-REPORT.md" .claude/agents/supervisor.md  # Report handling
grep "Spawning the Diagnostic Specialist" .claude/agents/supervisor.md  # New section exists
grep "skill/diagnostic/SKILL.md" .claude/agents/supervisor.md  # Cross-reference
wc -l .claude/agents/supervisor.md  # Should be ~430-450 lines (was 383)
```
  </verify>
  <done>
.claude/agents/supervisor.md updated with: (1) updated YAML description mentioning diagnostic specialist, (2) new "Spawning the Diagnostic Specialist" section with when/how/after workflow, (3) monitoring section updated with diagnostic escalation path, (4) domain knowledge references updated, (5) new important rule about not spawning for routine iterations. Existing sections preserved with minimal changes.
  </done>
</task>

</tasks>

<verification>
Phase 25 Plan 02 verification:
1. skill/supervisor/SKILL.md has new diagnostic specialist delegation section (~80-120 new lines)
2. .claude/agents/supervisor.md has new "Spawning the Diagnostic Specialist" section (~40-60 new lines)
3. Delegation threshold is consistent between both files (2 failed interventions)
4. Task tool invocation template is present in skill/supervisor/SKILL.md
5. Post-diagnostic workflow documented (read report, advise CASE agent)
6. Both files cross-reference skill/diagnostic/SKILL.md
7. "Phase 25 will add..." placeholder note updated in both files
8. No content duplicated between supervisor skill and diagnostic skill
</verification>

<success_criteria>
- Supervisor knows when to spawn diagnostic specialist (2 failed interventions threshold)
- Supervisor has Task tool invocation template for diagnostic specialist
- Supervisor reads DIAGNOSTIC-REPORT.md and uses it to formulate advisory for CASE agent
- Supervisor does NOT spawn diagnostic specialist for routine iterations
- Both supervisor files (agent + skill) are updated consistently
- Cross-references to skill/diagnostic/SKILL.md present without content duplication
</success_criteria>

<output>
After completion, create `.planning/phases/25-diagnostic-specialist/25-02-SUMMARY.md`
</output>
